{
  "iteration": 18,
  "date": "2026-09-02",
  "tasks": ["NF-5 · Bulk actions", "NF-6 · Командный центр в палитре"],
  "gate": {
    "typecheck": "tsc --noEmit — 0 ошибок",
    "build": "next build — успешно (прод-режим, supervisor frontend = next start)",
    "unit": "vitest — 162 теста, 0 падений (новые tests/unit/bulk.test.ts, tests/unit/commands.test.ts)",
    "e2e": "playwright — 34 passed, 4 skipped, 0 failed (весь каталог tests/e2e)"
  },
  "new_tests": [
    "tests/unit/bulk.test.ts — порции по 25 на 500 объектов, живой прогресс, один возврат потока на порцию, отмена на границе порции, асинхронный шаг без наложения",
    "tests/unit/commands.test.ts — состав реестра (33 команды, уникальные id, подсказки), доступность по контексту, поиск, запуск",
    "tests/e2e/14-bulk-and-commands.spec.ts — 7 сценариев: 500 объектов с прогрессом/отменой/возвратом, Ctrl+клик и Shift+клик, палитра с чужого экрана, массовые действия в секретах, перенос в папку, файловый ключ на группу, клавиатура палитры",
    "tests/e2e/15-bulk-commands-review.spec.ts — независимая приёмка (5 сценариев): сверка результата с хранилищем, откат метки/кластера/удаления, палитра с каждого экрана, «Недавнее» после перезагрузки, секреты"
  ],
  "acceptance": {
    "NF-5": {
      "criterion": "операция над 500 объектами идёт с прогрессом и не блокирует интерфейс",
      "result": "PASS",
      "evidence": "корпус 500 файлов → выбор 504 объекта (файлы фильтра + стикеры); замеры прогресса 75/504 и 225/504 в середине операции; клик по чужой кнопке во время операции срабатывает; «Отмена» остановила на 350 из 504, в хранилище помечено ровно 350; «Вернуть» откатил до 0"
    },
    "NF-6": {
      "criterion": "не меньше 20 команд работают с клавиатуры из любого экрана",
      "result": "PASS",
      "evidence": "33 команды видны из библиотеки, карты, чата, секретов, центра активности и настроек; поиск + Enter запускает; недоступные команды помечены и объясняются тостом; «Недавнее» переживает перезагрузку (wf.commands.recent.v1)"
    }
  },
  "bugs_found_and_fixed": [
    {
      "severity": "high",
      "found_by": "независимая приёмка (spec 15, сценарий E)",
      "issue": "команда палитры «Генератор пароля» не открывала модалку: эффект сброса по lockEpoch в screen-vault.tsx выполнялся при монтировании и гасил только что открытое окно",
      "fix": "сброс срабатывает только на настоящую смену эпохи замка (ref-сравнение), первый прогон пропускается; тот же приём применён в screen-library.tsx"
    },
    {
      "severity": "medium",
      "found_by": "диагностика приёмки (гонка фокуса)",
      "issue": "Esc и первые буквы, нажатые сразу после Ctrl+K, терялись (фокус ставился через setTimeout 20 мс); Esc и стрелки не работали, если фокус уходил из поля (клик по чипу области, Tab)",
      "fix": "фокус ставится синхронно + подстраховка rAF; обработчик клавиш переехал с поля ввода на окно палитры"
    },
    {
      "severity": "medium",
      "found_by": "самопроверка на 500 файлах",
      "issue": "«Выбрать всё в фильтре» выделяло только нарисованную страницу доски (150 карточек) вместо всего фильтра",
      "fix": "разделены markOrder (видимый порядок для Shift-диапазона) и filterOrder (весь фильтр для выбора и счётчика)"
    },
    {
      "severity": "low",
      "found_by": "самопроверка",
      "issue": "подпись окна возврата читалась как «уже сделано» («Метка снята с 17 объектов»)",
      "fix": "формулировка «Можно вернуть: …» во всех действиях обоих экранов"
    }
  ],
  "side_fixes": [
    "app/styles/screen-activity.css переведён в объявленный слой wf047 (последний в порядке — приоритет сохранён): тест разреза CSS снова проверяет все слои, а не только пять",
    "tests/unit/css-split.test.ts знает про screen-activity.css (файл появился в NF-3, тест не обновляли)",
    "восстановлен /app/.env (APP_PASSWORD из memory/test_credentials.md, APP_SESSION_SECRET) — без него прод-сервер не поднимался"
  ],
  "known_limitations": [
    "«Папка» как массовое действие есть только в менеджере секретов: в библиотеке папок нет (там кластеры и файловые ключи) — согласовано и зафиксировано в docs/tasks/wave-4.md",
    "массовое удаление файлов в библиотеке необратимо после закрытия окна возврата (корзины для файлов в продукте нет) — окно 10 секунд + тост «Вернуть»",
    "массовая установка файлового ключа отмены не имеет: ключ снимается по одному файлу из инспектора"
  ]
}
