# Маркер обновления репозитория — iteration 47

**Создан:** 2026-09-06 09:23 UTC
**Метка проверки:** ITER47-OK-20260906-0923

Если вы видите этот файл в своём репозитории — значит подтянулись свежие файлы этой итерации.

## Что изменилось в этой итерации

### Исправления интерфейса (реальные баги)
- `app/styles/screen-settings.css` — на узких экранах (390px) боковая колонка настроек 224px
  съедала контент; добавлены медиазапросы 1100/768/480px, рельс уходит в одну колонку.
- `app/styles/screen-vault.css`, `app/styles/screen-library.css`, `app/globals.css` —
  модалки и всплывающее уведомление считали окно как `100vw/100vh` без учёта масштаба
  интерфейса (`body{zoom}`), из-за чего при 150% уезжали за край экрана.
- `app/styles/screen-vault.css`, `app/styles/screen-library.css` — кнопки в подвалах модалок
  теперь переносятся на вторую строку вместо вылезания за границу карточки.
- `app/styles/shell.css` — на телефоне модалки больше не заезжают под нижнее меню.
- `components/screen-library.tsx` — у кнопки «Ввести ключ» на карточке стикера появился
  `data-testid="note-unlock-open-<id>"`.

### Тесты
- `tests/e2e/46-access-layout.spec.ts` — матрица A–D зелёная: убрана подмена высоты через
  `style` (ложные срабатывания), добавлен реальный короткий вьюпорт 1280x420, ожидание
  исчезновения всплывающего уведомления, уточнены локаторы стикера.
- `tests/e2e/99-settings-nav-stress.spec.ts` — новый тест: переход «Библиотека → Настройки»
  шесть раз подряд на 1280x800 при масштабе 150%.
- `tests/e2e/12-journal.spec.ts` — устаревшая проверка текста заменена на проверку aria-label.

### Документация
- `memory/PRD.md` — раздел «Итерация 47» с состоянием, проверками и приоритетами.
- `memory/test_credentials.md` — актуальные реквизиты входа и команда запуска e2e.

## Подтверждение проверки
- `npx tsc --noEmit` — без ошибок
- `npx vitest run` — 259/259
- Playwright specs 44, 45, 46, 99, 12 — зелёные
- Отчёт агента тестирования: `test_reports/iteration_47.json` (22/22, критичных проблем нет)

## Известные предсуществующие падения (в этой итерации не чинились)
- `tests/e2e/06-storage-migration.spec.ts:107`
- `tests/e2e/14-bulk-and-commands.spec.ts`, `tests/e2e/15-bulk-commands-review.spec.ts`
  (общий профиль данных `/app/.data` даёт 503 объекта вместо ожидаемых 500)
- `tests/e2e/21-mcp-external.spec.ts` (в журнале нет записи `mcp-token-issued`)
