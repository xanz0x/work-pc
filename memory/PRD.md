# PRD · WorkSpaceX · Менеджер секретов (модуль VAULT)

## Исходная задача
`docs/product/original-brief.md`: реализовать в существующем прототипе `vault-core-arch`
(Next.js 16 / React 19 / TS strict / Tailwind v4, local-first, дизайн «Графит») модуль
«Менеджер секретов» — менеджер паролей, TOTP и seed-фраз ПОВЕРХ существующей
криптосистемы замка (PBKDF2 → AES-GCM), не ломая ничего существующего.

## Решения владельца (сессия 2026-06, ask_human)
1. Объём: только CORE v1 (шаги 2–16 §6 промта).
2. KDF 310 000 → 600 000 разрешено, с ленивой ре-миграцией при первом unlock.
3. Zero-dependency — закон (ни одной новой npm-зависимости не добавлено).
4. Иконки сайтов через `google.com/s2/favicons` — реализовано, но тумблер по умолчанию ВЫКЛЮЧЕН
   (рекомендация агента: иначе домены записей утекают в Google).
5. Проверка — на preview-URL этой среды (порт 3000, `next build && next start`).

## Архитектура (что где лежит)
- `lib/secrets.ts` — модель, шаблоны 12 типов, фильтры `type:/tag:/favorite:/expired:`, миграция формата.
- `lib/secrets-crypto.ts` — SEK (32B, завёрнут мастер-ключом) → HKDF на запись → AES-GCM `ct:iv`;
  портируемые снимки (PBKDF2 600k) и бэкапы под мастер-ключом.
- `lib/secrets-totp.ts` — RFC 6238/4226 на WebCrypto, Base32, `otpauth://`.
- `lib/secrets-gen.ts` — CSPRNG-генераторы, локальный strength-meter.
- `lib/secrets-io.ts` — CSV/Bitwarden JSON импорт (валидация, лимиты), экспорт.
- `lib/secrets-store.tsx` — `SecretsProvider`/`useSecrets`: CRUD, папки, корзина, история,
  буфер с автоочисткой, бэкапы, favicon, panic-lock.
- `lib/lock-migrate.ts` — KDF 310k→600k + `rewrapAll` (файловые ключи, секреты стикеров, SEK).
- `components/screen-vault.tsx` + `components/vault/*` — экран из трёх колонок.
- `components/secrets-section.tsx` — секция настроек модуля.
- Слой стилей `/* ===== VAULT v1 ===== */` в конце `app/globals.css`.
- Точки правки каркаса: `ScreenId 'vault'`, `PLACEHOLDER`, `workspaceCount`, `statusText`,
  `page.tsx`, `HitKind 'secret'`, `HIT_ICON/KIND_LABEL`, ветка `runHit`, `SECTIONS`/`FOCUS_ALIAS`/`SETTING_ENTRIES`.

## Что сделано (2026-06, CORE v1)
- Крипто-слой поверх замка, KDF поднят до 600k с ленивой миграцией и бэкапом старой схемы.
- Смена мастер-ключа теперь честно пере-упаковывает файловые ключи и секреты стикеров (был пробел).
- Экран «Менеджер секретов»: навигация/список/деталь, 12 типов, конструктор полей,
  папки, теги, избранное, корзина с восстановлением и безвозвратным удалением.
- Генератор (пароль/PIN/hex/UUID) + strength-meter; TOTP с отсчётом и импортом otpauth.
- Буфер обмена с автоочисткой по типам, тост с отсчётом, «Очистить сейчас».
- Показ секрета с авто-скрытием, гашение при уходе со страницы/блокировке.
- Импорт CSV (KeePassXC и др.) и Bitwarden JSON с превью; экспорт зашифрованный и plaintext
  с подтверждением; бэкапы под мастер-ключом с ротацией и restore.
- Секреты в глобальном поиске и Ctrl+K как вид `secret` (только название/тип/теги).
- Panic Lock+ (Ctrl+Shift+L): замок + очистка буфера + гашение раскрытий; дубль хоткея убран.
- Секция настроек модуля; тумблер изоляции от ИИ включён и заблокирован.

## Приёмка (выполнено)
- `npx tsc --noEmit` → exit 0.
- TOTP сверен с официальными векторами RFC 6238 (`node scripts/totp-check.ts` после компиляции) — 6/6.
- E2E на preview: создание записи → шифрование → показ → копирование (буфер прочитан из браузера:
  значение появилось, после Ctrl+Shift+L буфер пуст) → корзина → восстановление.
- Импорт KeePassXC CSV: превью 3 записи (1 с TOTP) → импорт 3 → авто-создание папок Root/Work, Root/Crypto.
- Бэкап → restore → «Восстановлено 3 записей»; зашифрованный экспорт скачивается.
- F5 → экран блокировки → unlock → секреты снова расшифровываются.
- Смена мастер-ключа → секрет по-прежнему расшифровывается (путь `rewrapAll`).

## Не сделано (осознанно, backlog)
Подробная передача дел — в `/app/docs/architecture/secrets-vault.md` (что сделано, как запускать, что осталось).
P0: живая проверка ленивой миграции KDF 310k→600k (ядро `rewrapAll` проверено через смену мастер-ключа).
P1 (v1.1 по промту): Password Health (локальный score), expiration-напоминания и бейджи в списке
(бейдж EXPIRED реализован, напоминаний нет), BIP39-генератор и checksum, Generator Hub
(passphrase/username/token), история паролей с откатом (снапшоты пишутся, UI откатa нет),
Wi-Fi QR, вложения к записям, drag&drop папок.
P2: виртуализация списка при 1000+ записей, отдельный аудит-журнал модуля (пока пишем в общую ленту).
Вырезано по ТЗ §3.3: duress vault, полноценный WebAuthn, .kdbx, HIBP, team/shared, генерация SSH-пар, autofill.

## Как запускать в этой среде
```
npx next build && npx next start -p 3000 -H 0.0.0.0     # preview-URL
npx tsc --noEmit                                        # главный гейт
```
`next dev` в этой среде отдаёт чанки с 403 из-за проверки cross-origin у Next 16 —
для проверки используется production-сборка. Хосты preview добавлены в `allowedDevOrigins`.

## Сессия «дизайн-правки VAULT v1.1» (июнь 2026) — все 6 пунктов сделаны, приёмка iteration_2: 100% PASS
1. «Кластеры» убраны из сайдбара — выпадающий список у «Библиотеки» (шеврон `nav-library-toggle`).
2. Папки сейфа удаляются (корзина по ховеру, подтверждение вторым кликом, записи остаются без папки).
3. Генератор: параметры пароля — явные тумблеры с чекбоксами, включённые видны сразу; проверено, что выключенные наборы символов реально исключаются.
4. Редактор записи: полный редизайн «Графит» — сетка типов с иконками, звезда-избранное, компактные поля-карточки с тумблером ct, стилизованные select, дата «Истекает» — текстовый ввод ДД.ММ.ГГГГ с автоточками и валидацией (нативный календарь убран).
5. Иконки всех 12 типов записей (новые SVG: карта, Wi-Fi, seed, терминал, отпечаток; палитра TYPE_HUE) в списке/детали/типах; favicon по ссылке — поверх, когда включён тумблер.
6. Починены таймеры: авто-скрытие показанного секрета и автоочистка буфера (нормализация настроек дефолтами против NaN; отложенная очистка буфера при возврате фокуса окна — браузер не даёт писать в clipboard без фокуса; очистка через пробел в execCommand-fallback).
Бонус: favicon.ico + icon.svg (0 ошибок в консоли), @vercel/analytics отключён вне Vercel.

## Сессия «VAULT v1.2» (июнь 2026) — favicon-фикс + 4 фичи, приёмка iteration_3 (4/5) + ручная доводка favicon → всё зелёное
0. Favicon для записей со ссылкой: включён по умолчанию (старые снимки настроек мигрируются, ручной выбор уважается через faviconsSet); загрузка через same-origin `/proxy/favicon?domain=` (route handler Next, наружу только домен) с фолбэком на Google S2. Путь НЕ /api/* — ингресс preview уводит /api на несуществующий backend:8001.
1. Password Health: вид «Здоровье паролей» в левой колонке — слабые (score≤1), повторные, старые (180+ дн), общий балл, клик ведёт к записи. Аудит локальный, plaintext живёт только на время расчёта.
2. BIP39: режим «Seed BIP39» в генераторе — 12/24 слова, словарь встроен (lib/bip39-words.ts), алгоритм сверен с офиц. векторами BIP39; живая проверка контрольной суммы вставленной фразы (длина/словарь/чексумма — три разных ошибки).
3. Password History: секция «История изменений» в детали — показ прежнего значения (с автогашением по revealSeconds) и «Вернуть» одним нажатием; откат двусторонний (текущее уходит в историю), без пере-шифрования (ct того же ключа записи).
4. Expiry Reminders: уведомления в ленту за 30/7/1 день и при просрочке; дедуп в wf.secrets.expiry.v1 (ключ включает дату — перенос срока сбрасывает), работает даже при закрытом замке (метаданные плейн).

## Сессия «VAULT v1.3» (июнь 2026) — 3 правки, приёмка iteration_4: 100% PASS
1. Папки: удаление стало очевидным — клик по корзине превращает кнопку в явную красную «Удалить?» (5 с), второй клик удаляет (логика и раньше работала, UX был неочевиден).
2. Сайдбар: раскрытые кластеры теперь рендерятся сразу ПОД «Библиотекой» (Fragment внутри WORKSPACE.map), а не после «Карта памяти»/«Чат с ИИ».
3. Редактор записи: компактная сетка типов 6 колонок с короткими подписями (SHORT), кастомные выпадающие списки VtSelect (components/vault/vt-select.tsx — переиспользуемый: кнопка + listbox, Esc/клик-вне; testid опций `<testId>-opt-<value>`), поля в одну строку, модалка помещается в экран (max-height: calc(100vh-32px), внутренний скролл). Смоук: сохранение с папкой через дропдаун + дата + favicon — зелёное.

## Сессия «VAULT v1.5» (июнь 2026) — 4 фичи P1, приёмка iteration_5: 100% PASS, замечаний нет
1. **Wi-Fi QR**: собственный кодер QR (`lib/qr.ts`, zero-dependency) — модель 2, байтовый режим UTF-8,
   уровень M, версии 1–10 (до 213 байт): GF(256), Рида-Соломона, чередование блоков, выбор маски
   по 4 правилам штрафов, format/version info. Проверен независимым декодером (OpenCV QRCodeDetector)
   на версиях 1–10 и на кириллице. UI: кнопка `detail-wifi-qr` только у записей типа Wi-Fi →
   модалка `wifi-qr-modal` (SVG-модули, автозакрытие 45 с, перерисовка, закрытие по panic-lock).
   Секрет в разметку не попадает — только модули (проверено тест-агентом).
2. **Generator Hub**: к 5 режимам добавлены «Фраза» (passphrase из словаря BIP39, 3–10 слов,
   разделитель через VtSelect, заглавные, число; энтропия считается как n·log2(2048)) и
   «Имя» (произносимые пары слогов, 2–4 блока, точка, цифры).
3. **Вид «Истекающие»**: пункт `vault-view-expiring` рядом со «Здоровьем паролей», счётчик
   с предупреждающим цветом; группы «Просрочено» / «Скоро истекут» (≤30 дн) / «С запасом»,
   бейджи EXPIRED и «N дн», сортировка по сроку, клик ведёт к записи. Расшифровки нет —
   только метаданные (`expiryStage`/`expiringCount` в `lib/secrets.ts`).
4. **Вложения**: `addAttachment`/`removeAttachment`/`downloadAttachment` в store — файл шифруется
   AES-GCM ключом записи (mime|base64 через `sealField`), лимит 256 КБ на файл и 1 МБ на запись;
   скачивание — расшифровка в память → Blob → отзываемый object URL. UI: `vault-attachments.tsx`
   в детали записи. Подтверждено: маркер из файла в localStorage отсутствует.
CSS дописан слоем «VAULT v1.5» в конец `app/globals.css`, палитра «Графит» не менялась.

## Бэклог после v1.5
- P0: живая проверка ленивой миграции KDF 310k→600k (код готов, сценарий в docs/architecture/secrets-vault.md).
- P2: drag&drop записей в папки; переименование папок из UI (`renameFolder` уже есть);
  виртуализация списка при 1000+ записей; отдельный аудит-журнал модуля секретов.
- Вырезано осознанно: duress vault, WebAuthn, .kdbx, HIBP, team/shared, autofill.

## Сессия «MAP v2 · Космос и автопоток» (июнь 2026) — приёмка iteration_6: 100% PASS, замечаний нет
Запрос пользователя: карта памяти должна работать как готовое приложение, кнопки «Проиграть поиск»
быть не должно (волны идут сами), фон-сетку заменить на тёмный красивый космос.
1. **Автопоток волн активации.** Кнопка удалена. В движке появился планировщик
   (`scheduleAuto`/`runAuto`/`autoTarget` в `components/screen-map.tsx`): волна каждые
   SEARCH_END+4.2 с, цель — сначала результаты живого поиска, затем хабы по степени связности
   (ротация, чтобы показывались разные узлы). Планировщик уступает человеку: пока тянут карту,
   водят курсором по узлам или зумят — ждёт (IDLE_GUARD 2.2 с), при возврате на вкладку
   возобновляется. Ручной клик по узлу закрепляет карточку (`userPinned`) — автоматика больше
   не подменяет инспектор, но волны продолжают идти. prefers-reduced-motion: волны без анимации.
   Живой поиск в шапке поднимает волну к верхнему результату за 260 мс.
2. **Космос вместо сетки.** `.map-grid` убран из разметки; добавлен слой `.cosmos`:
   глубокий градиент, три дышащие туманности (screen-blend), млечная полоса, три параллаксных
   звёздных слоя с мерцанием, виньетка. На канвасе — звёздное поле (60–150 звёзд, крупные с
   лучами и тёплым/холодным тоном) и редкие метеоры; виньетка и притухание перекрашены в
   космический синий. Панели над картой получили плотнее стекло, чтобы текст читался.
3. **Инспектор как в реальном приложении.** Живой индикатор «наблюдение/волна», секция
   «Связи узла · N» — настоящие соседи из `neighborsOf(graph)` с причиной связи
   (приколот/метка/кластер), весом и переходом по клику; для ядра — «Магистрали к кластерам»
   (клик = фильтр кластера); «Поток активации · авто» с живым таймлайном и журнал
   «Найдено в этом сеансе» (до 3 целей, клик возвращает к узлу).
4. **Мелочи удобства.** Ambient-пилюля состояния потока со счётчиком волн над картой;
   подписи кластеров теперь держатся до 1.3× зума (карта читается на дефолтном масштабе);
   подсказка в футере «волны идут сами…»; data-testid на пилюлю, зум, инспектор и строки связей.
CSS дописан слоями «MAP v2 · КОСМОС» и «MAP v2.1» в конец `app/globals.css`.
Известный долг (не блокер, из code-review тест-агента): `screen-map.tsx` разросся до ~1.9 тыс.
строк — стоит разбить на модули (планировщик / рендер канваса / инспектор / обвязка UI).

## 2026-06 — Запуск превью + фикс ИИ-чата
- Установлены зависимости (pnpm 9, `--ignore-workspace`); добавлен лончер `/app/frontend/package.json` (supervisor ждёт фронт в /app/frontend, приложение лежит в /app) -> `next dev` на 0.0.0.0:3000.
- Фикс «ничего не кликабельно»: Next 16 блокировал /_next/static чанки -> в `next.config.mjs` добавлены wildcard `allowedDevOrigins` (*.preview.emergentagent.com, *.preview.emergentcf.cloud).
- Создан `/app/.env`: AI_PROXY_URL=https://integrations.emergentagent.com/llm, EMERGENT_LLM_KEY (Emergent Universal Key). Модель claude-opus-5, стриминг SSE + tool calling работают.
- Фикс композера: черновик до создания сессии (freeDraft) в `components/screen-chat.tsx`.
- Фикс ветвления запросов: синхронизация idx с variants в `components/chat/message-user.tsx`.
- Примечание: supervisor `backend` (FastAPI) в BACKOFF — server.py нет, вся логика в Next.js route handlers. Ожидаемо.

### Бэклог
- P1: лейбл автора ответа «ЛОКАЛЬНАЯ МОДЕЛЬ» при облачном Opus 5 — вводит в заблуждение.
- P2: статус-бар пишет «ГИБРИДНЫЙ РЕЖИМ» при выборе «Внешняя модель» (app-shell.tsx ternary на stats.offline).
- P2: адаптив 768px — обрезка подсказки мастер-ключа, низкий контраст disabled CTA.

## 2026-06 — Экспертный аудит + новая модель уведомлений
### Аудит
- Проведён полный аудит по брифу пользователя (product_audit_approval_brief.pdf): продукт, UX/UI, функции, логика, архитектура, данные, безопасность, надёжность, performance, релизная готовность.
- Итоговая оценка: 48/90 — продвинутый прототип с продакшн-ядром безопасности на клиенте. 5 блокеров P0.
- Артефакт: интерактивный PDF `/app/public/WorkSpaceX-Audit-2026-06.pdf` (31 стр., 183 fillable-поля), доступен по {preview}/WorkSpaceX-Audit-2026-06.pdf. Генератор: `scripts/build_audit_pdf.py` + контент `scripts/audit_content.py` (пересборка: `python3 scripts/build_audit_pdf.py`).
- Ключевые P0: P0-1 обещание приватности расходится с фактом (индекс сейфа уходит в облако, лейблы врут); P0-2 /ai-api/* без авторизации (чужие диалоги, системный промпт, MCP-токен, расход LLM-ключа); P0-3 localStorage как единственный источник истины; P0-4 ноль автотестов + ignoreBuildErrors; P0-5 уведомления (ЗАКРЫТО).
- Backlog решений в PDF: UX-1..UX-5, LG-1..LG-5, AR-1..AR-5, RM-1..RM-3, NF-1..NF-11, roadmap из 5 волн.

### Реализовано: уведомления v5.0 (P0-5, пункты N-1, N-2, N-3, N-5, N-7)
- Раздельные действия: клик по телу = открыть источник + прочитать (openNotif с link на файл/стикер/секрет/настройку), точка-статус = двусторонний toggle read/unread.
- Архив вместо безвозвратного удаления: «Убрать», «Очистить прочитанные», «Очистить всё», «Стереть архив» — все с отменой 7 с (notifUndo/undoNotifs). Безвозвратное удаление только внутри архива.
- Retention: 200 активных / 300 архивных / TTL 30 дней (pruneNotifs) вместо жёсткого лимита 40.
- Флаг `wf.notifs.seeded.v1`: очищенная лента больше не возрождает демо-события.
- Счётчики чипов = непрочитанные; один фильтр вместо вкладок + тумблера; выключенная категория помечается точкой.
- Синхронизация вкладок через BroadcastChannel `workflow-notifs`; aria-live на счётчик; пагинация «Показать ещё» по 30.
- Приёмка тест-агентом: iteration_10.json — 100% PASS, дефектов нет.

### Приоритеты дальше (ожидают утверждения владельца)
- Волна 1 (P0): P0-1 честный режим приватности, P0-2 auth+rate limit на /ai-api, UX-1 единый источник лейблов, LG-2 каталог ошибок.
- Волна 2 (P0–P1): P0-3 IndexedDB-репозитории, P0-4 тесты и CI, UX-2 состояния ошибок, LG-1 окно контекста, AR-5 логи и валидация env.
- Волна 3 (P1): NF-1 реальный индексатор файлов, NF-2 локальный движок, AR-1 разделение стора, AR-2 code splitting, NF-4 онбординг, LG-3 журнал безопасности.

## 2026-06 — Уборка репозитория (пункт AR-4 аудита)
- Создан каталог `docs/` как единственное место для прозы: `docs/README.md` (индекс), `docs/architecture/` (lock-system.md, secrets-vault.md), `docs/product/` (original-brief.md, secrets-vault-plan.md/.pdf, secrets-vault-inventory.md), `docs/audit/` (PDF аудита + инструкция по пересборке).
- Корень репозитория: только README.md, AGENTS.md, CLAUDE.md + конфиги. Было 7 md-файлов и каталог планов.
- Удалено: `NEXT-CHAT.md` (устаревший хендовер), `secrets-vault-plan/report.html` и `build_report.py` (генерируемое), `hooks/use-fake-stream.ts` (мёртвый код), `lib/utils.ts` + `components.json` (shadcn не используется), `test_reports/pytest/*` (артефакты), `ai/sessions/*.json` (тестовые диалоги), кеши `.pnpm-store` (435 МБ), `.ruff_cache`, `scripts/__pycache__`, `frontend/node_modules`.
- Из package.json убраны неиспользуемые зависимости: `shadcn`, `clsx`, `tailwind-merge`.
- Тесты API от QA-агента переехали в `tests/api/test_ai_api.py`, BASE_URL читается из `APP_URL`; добавлен `tests/README.md`.
- Пояснительные README добавлены в `backend/` (намеренно пустой) и `frontend/` (лончер для supervisor).
- `.gitignore`: добавлены `ai/sessions/*.json` (приватные диалоги не в репозитории), `.pnpm-store/`, `__pycache__/`, `.ruff_cache/`.
- README обновлён: раздел «Документация», актуальное дерево проекта, ссылки на новые пути.
- Проверка: `npx tsc --noEmit` = 0, `npx next build` = успешно (12 маршрутов), регресс тест-агентом iteration_11.json = 100% PASS, ссылок на удалённые модули в коде нет.

## 2026-06 — Аудит утверждён, план разложен на задачи
- Владелец продукта утвердил PDF-аудит. План работ разложен в `docs/tasks/`:
  `README.md` (доска со сводной таблицей 32 задач и статусами), `HOW-TO-WORK.md` (как начинать сессию: что читать, правила, чем заканчивать), `wave-1.md` … `wave-5.md` (задачи с блоками «Проблема → Файлы → Шаги → Готово когда»).
- Порядок: Волна 1 (P0-1 честный режим приватности, P0-2 auth+rate limit, UX-1 подписи режима, LG-2 каталог ошибок, N-хвост уведомлений, RM-2 метрики движка) → Волна 2 (данные и тесты) → Волна 3 (индексатор, локальный движок, разрезание стора, code splitting, онбординг, журнал) → Волна 4 (зрелость) → Волна 5 (MCP наружу, E2EE-синк, телеметрия).
- DONE к этому моменту: P0-5 (уведомления), RM-1 (мёртвый код), AR-4 (структура и docs/).
- В этой сессии писался только план — код не менялся, проверки не запускались по просьбе пользователя.

## 2026-06-30 — Волна 1 · Правда и безопасность (P0-1, P0-2, UX-1, LG-2, N-хвост, RM-2) — DONE

### Решения владельца (ask_human этой сессии)
1. Объём: вся волна 1 целиком.
2. Вход: пароль приложения из `APP_PASSWORD` + httpOnly-cookie сессии (не Google Auth). Пароль задан владельцем: `IceKrymTeam13@`.
3. Лимиты: 10 ходов/мин и 200/сутки с адреса.
4. `AI_DIR` на выбор агента → `/root/.workflow/ai` (вне репозитория, переживает перезапуски пода).
5. Облачная модель: `claude-opus-5` недоступна на текущем плане ключа → переведено на `claude-sonnet-4-5-20250929`, подпись во всём интерфейсе «Claude Sonnet 4.5» (`AI_MODEL` + `NEXT_PUBLIC_AI_MODEL_LABEL`).
6. Индекс сейфа по умолчанию отправляется, но только после согласия и с записью в ленту.

### P0-1 · Честный режим приватности
- `app/ai-api/chat/route.ts`: сервер сам решает по полю `engine`; при `local` → 409 `ENGINE_NOT_CONFIGURED` без единого внешнего запроса.
- Тумблер `sendIndex` («Отправлять индекс сейфа во внешнюю модель») в «Настройки → Приватность»; при выключенном индексе системный промпт честно сообщает, что индекс не передан.
- `components/chat/cloud-consent.tsx`: модалка согласия с точным перечнем того, что уйдёт наружу; согласие пишется в профиль (`cloudConsentAt`), видно и отзывается в настройках.
- Каждый облачный ход → событие ленты категории «Приватность»: «В облако ушло N имён файлов».

### P0-2 · Вход и лимиты
- `proxy.ts` (Next 16 конвенция вместо `middleware.ts`) закрывает `/ai-api/*`, кроме `/ai-api/auth/*` → 401 `AUTH_REQUIRED`.
- `lib/app-auth.ts`: HMAC-SHA256 подпись cookie на WebCrypto (edge-совместимо), сравнение хешей без утечки времени, TTL 12 ч. Ноль новых зависимостей.
- `app/login/page.tsx` + `/ai-api/auth/login|session`: вход, состояние, выход. В чате появляется чип «нужен вход», карточка ошибки даёт кнопку «Войти».
- `lib/rate-limit.ts`: 10/мин + 200/сутки на `/ai-api/chat` (429 + `Retry-After`), 10 неудачных входов на 15 минут (успешный вход обнуляет счётчик).
- Секреты убраны из файлов: `MCP_NOTION_TOKEN` живёт только в окружении, API отдаёт лишь `tokenSet`; `AI_DIR` вынес диалоги из репозитория.

### UX-1 · Единый источник подписей
- `engineView` в `lib/vault-store.tsx` (mode/label/model/isCloud/ready/statusLabel/netLabel/consented) — читают статус-бар, engine-pill, шапка чата, футер композера и автор ответа. Автор берётся из фактического источника хода (`AiMsg.via`).

### LG-2 · Каталог ошибок
- `lib/ai-errors.ts`: 9 кодов → заголовок + подсказка + признак «можно повторить». Наружу уходит только код и фраза, детали и `requestId` — в журнал сервера. Ретрай с бэкоффом (2 попытки) и таймаут 60 с на стороне сервера; в UI — кнопки «Повторить», «Сменить движок», «Войти».

### N-хвост · Уведомления
- N-4: retention больше не выбрасывает события приватности (исключены из обрезки активной части).
- N-6: сводка хранит склеенные события (`items`) и раскрывается кнопкой в панели.
- N-9: панель не закрывается от прокрутки (обработчика внешней прокрутки в коде нет — проверено).
- N-10: действия «Отложить на час» (`snoozedUntil`) и «Выключить категорию» прямо из уведомления.

### RM-2 · Метрики без выдумки
- `MODELS[].ram/tokensPerSec` = `null`, `LOCAL_ENGINE_READY = false`, в UI «—»; статус «ЗАГРУЖЕНА» заменён на «локальный движок не подключён»; уведомления о смене модели/движка переписаны честно.

### Проверка
- `npx tsc --noEmit` = 0 ошибок, `npx next build` = успешно (14 маршрутов).
- `tests/api/test_wave1.py` (12 тестов, новые) и обновлённый `tests/api/test_ai_api.py` (15 тестов) — всё зелено локально.
- Тест-агент: `test_reports/iteration_12.json` — 100% PASS по бэкенду и UI (вход, 401/409/429, согласие, событие приватности, подписи режима, действия уведомлений, регресс экранов).

## Волна 2 · Данные и устойчивость — DONE 2026-06-30

Цель волны выполнена: данные пользователя больше не зависят от 5-мегабайтного
localStorage, а регрессии видны до релиза.

### P0-3 · IndexedDB-репозитории и миграции
- `lib/db/*`: `schema.ts` (версии, раскладка ключей), `idb.ts` (обёртка + апгрейд v1→v2),
  `repo.ts` (единый интерфейс `get/put/patch/remove/list` + версия схемы), `migrate.ts`
  (перелив `wf.*` из localStorage с бэкапом до подтверждённого чтения и уборкой на
  следующем запуске), `quota.ts` (`estimate` + `persist`), `errors.ts` (шина ошибок записи),
  `persist.ts` (адаптер для `usePersistedState`).
- `hooks/use-persisted-state.ts` переписан на асинхронное чтение; внешний контракт
  `[value, set, hydrated]` сохранён, экраны и `useVault()` не менялись.
- В localStorage осознанно остались: конфиг и состояние замка, обёртки файловых ключей,
  SEK, активный экран, черновики, позиции скролла — их читает bootstrap синхронно.
  Инвариант «locked ⇒ шифртекст существует» покрыт тестом.
- `components/storage-alert.tsx`: «не сохранилось» с повтором и отдельный экран «нет места».

### P0-4 · Тесты и CI-гейт
- Скрипты `lint` / `typecheck` / `test` / `test:api` / `test:e2e`; `typescript.ignoreBuildErrors` убран.
- 68 unit-тестов (Vitest) — крипто-ядро, секреты, переупаковка ключей, аудит замка,
  доска, TTL стикеров, redact, уведомления, окно контекста, слой БД, env и лог.
- `tests/api/test_wave2.py` (10 тестов): request-id, 400 на битый JSON и плохой `sessionId`,
  трекер и метрики без утечки содержимого. Всего pytest — 37 тестов.
- 5 сценариев E2E (Playwright). CI-гейт `.github/workflows/ci.yml`.
- Предохранитель: ослабление KDF роняет `pnpm test` (проверено намеренной поломкой).

### UX-2 · Состояния ошибок
- `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`, `app/loading.tsx`,
  `components/screen-boundary.tsx` — падение экрана не роняет каркас, детали под раскрытием.

### LG-1 · Окно контекста
- `lib/context-window.ts`: последние 12 ходов и бюджет 24 000 символов, вытесненное
  сворачивается в системное резюме; лимиты 4 000 символов на ход и на результат скилла.
- Сервер отдаёт событие `ctx` в SSE, индикатор в шапке чата показывает реальную цифру.

### AR-5 · Логи, трекинг, env
- `lib/env.ts` + `instrumentation.ts`: сервер не стартует при неполной конфигурации.
- `lib/log.ts`: JSON-строки, whitelist полей (фильтр PII), хеш IP; `lib/route-log.ts` —
  request-id на каждом маршруте и в заголовке `X-Request-Id`.
- `lib/metrics.ts` + `/ai-api/telemetry`: ходы, ошибки, латентность p50/p95, токены
  (только реальные, присланные провайдером). Внешних сервисов нет.
- `.env.example` со всеми ключами и комментариями.

### Долг, оставленный сознательно
- `pnpm lint`: правила react-hooks 7 (purity/refs/set-state-in-effect) — предупреждения;
  снимутся вместе с AR-1 «Разделение стора и часов» (волна 3).
- Секреты и обёртки ключей не переезжали в IndexedDB: потребовался бы асинхронный
  bootstrap замка — вне рамок волны.

### Дальше (Волна 3 · Ядро продукта)
- NF-1 реальный индексатор файлов; NF-2 локальный движок (Ollama / WebGPU);
  AR-1 разделение стора и часов; AR-2 code splitting экранов; NF-4 онбординг;
  LG-3 журнал безопасности.

---

## 2026-06-30 · Хвост волны 2: перепроверка QA закрыта (сессия 3)

**Что закрыто (было «не подтверждено» в отчёте QA T1):**
- §1.1 в UI — новый `tests/e2e/09-file-key.spec.ts`: файл на ключ → reload →
  тот же пароль открывает; обёртка в `wf.filekeys.map.v1` (IndexedDB), ноль
  записей `wf.vault.keys.*` в localStorage. В `screen-library.tsx` добавлены
  `data-testid` для модалок файлового ключа.
- §1.2 — **найден и починен баг**: ранняя (до гидратации) запись подмешивала к
  архиву значение по умолчанию (20 → 34 файла). `hooks/use-persisted-state.ts`
  теперь запоминает ранние записи как операции и переигрывает их поверх
  прочитанного. Тест: `tests/e2e/10-first-frame-write.spec.ts` (21 файл).
- §1.3 — порядок миграции при входе через `/login` принят как поведение по
  умолчанию и зафиксирован вторым тестом в `06-storage-migration.spec.ts`.
- §1.4, §1.5 и регресс чата с облаком (03) — прогнаны, зелёные.
- `tests/e2e/idb.ts` — безопасное чтение IndexedDB из тестов (unversioned open
  создавал пустую базу v1 и ломал апгрейд).

**Среда после сброса пода:** восстановлены `node_modules` (pnpm), `/app/.env`,
`/app/.env.example`, браузеры Playwright в `/pw-browsers`; `AI_DIR` засеялся сам.

**Гейт:** tsc 0, eslint 0 errors / 85 warnings (долг AR-1), vitest 85, pytest 61,
playwright 14/14. Независимая перепроверка: `test_reports/iteration_13.json`.

**Дальше (P0/P1 бэклог):** волна 3 — AR-1 (вернуть правила react-hooks в error и
разобрать 85 предупреждений), AR-2 (code splitting + `app/loading.tsx`), NF-1
индексатор файлов, NF-2 локальный движок. Инфраструктура: первый настоящий
прогон CI на GitHub.

## 2026-06-30 · Перенос присланных макетов (сессия 3, продолжение)

Заказ: перенести макеты «Безопасность · МАСТЕР-КЛЮЧ» (PIN 6 ячеек / пароль) и
«НОВАЯ ЗАПИСЬ» (редактор сейфа) как есть по вёрстке, но в палитре «Графит».

- `components/security-section.tsx` — формы ключа переехали в модальное окно
  макета: шапка с иконкой и пульсирующей точкой «МАСТЕР-КЛЮЧ», сегмент
  PIN/пароль (активный — акцентная заливка), подписи со счётчиком справа,
  глаз у полей, баннер-предупреждение с кромкой, футер «Отмена / действие».
  Тем же каркасом идут «Смена ключа» и «Выключить замок» (danger-вариант).
- `components/vault/vault-editor.tsx` — сетка типов 6×2 иконкой вверх (активный
  с акцентной подчёркивающей кромкой), две строки шапки (название+папка,
  истекает+теги-чипы с «Добавить тег…»), блок «Поля записи»: строка = подпись
  слева, колодец значения с встроенными действиями (открыть ссылку / глаз /
  генератор / копировать) справа, полоска стойкости 2px под паролем, служебные
  кнопки (замок, тип поля, удалить) по ховеру с зарезервированным местом,
  «Добавить поле», блок «Конфигурация TOTP» с «Вставить», футер с отметкой
  о сквозном шифровании.
- Новые иконки: `IconEye`, `IconEyeOff`, `IconCopy`, `IconQrScan`,
  `IconAlertTri`, `IconDetails`. CSS — блоки «REDESIGN v4 / v4.1» в
  `app/globals.css` на наших токенах (префикс `vte-` у строк редактора, чтобы
  не конфликтовать с `.vt-row` карточки записи).
- Гейт после переноса: tsc 0, eslint 0 errors, vitest 85, playwright 14/14.

---

## 2026-08-31 · Волна 3, фаза 1 закрыта: AR-1 и AR-2

Заказ владельца: выполнить AR-1 (домены `data` и `lock` + цифры ререндеров) и
AR-2 (code splitting экранов). Решения из `ask_human`: фасад `useVault()`
обязателен; замер бандла — analyzer + вывод сборки, цифры в PRD; проверка
ререндеров — автотестом со счётчиками, а не ручным Profiler.

### AR-1 · Разделение стора и часов — DONE

- Новые домены: `lib/store/data.tsx` (корпус, стикеры, разговоры, граф,
  кластеры, `stats`) и `lib/store/lock.tsx` (мастер-ключ, анти-брутфорс,
  автоблокировка, синхронизация вкладок). Вместе с прежними `clock`, `toast`,
  `settings`, `notifs` сейф собран из шести независимых доменов.
- `lib/vault-store.tsx`: 1 642 → 601 строка. Осталось ровно то, что не
  принадлежит ни одному домену: навигация, поиск, сборка фасада `useVault()`.
  Внешний API не изменился ни на одно поле — это закреплено тестом контракта.
  Наружу дополнительно отданы узкие хуки `useDataStore`, `useLockStore`,
  `useSettingsStore`, `useNotifsStore`, `useToast`.
- Замок развязан с навигацией: `lockNow()` поднимает `lockEpoch`, фокусы
  сбрасывает фасад по эффекту (инвариант п.10.4 сохранён).
- Часы статус-бара вынесены в `components/ui/status-clock.tsx`; собственный
  `setInterval` в `AppShell` убран — каркас не перерисовывается раз в секунду.
- Производные считаются вне рендера: граф — отложенно (180 мс), состав живых
  стикеров — по грубому тику 5 с, содержимое индексатора — через
  `useSyncExternalStore`.

**Цифры ререндеров** (`tests/unit/store-renders.test.tsx`, jsdom + счётчики):

| Сценарий | Подписчик часов | Экран через `useVault()` |
|---|---|---|
| 3 тика секунды, только `ClockProvider` | 3 | **0** |
| 3 тика секунды внутри полного `VaultProvider` | 3 | **0** |
| `flash()` (домен тоста) | 1 | **0** (домен данных) |

Долг на будущее: экраны по-прежнему читают фасад целиком — перевод на узкие
хуки/селекторы делать по одному экрану, когда до них дойдут задачи волны 4.

### AR-2 · Code splitting экранов — DONE

- `components/screens.tsx`: пять экранов через `next/dynamic` (`ssr: false`,
  заглушка «Загрузка экрана…»); `app/page.tsx` не тянет ни один экран статически.
- Префетч: `prefetchScreen()` по `pointerenter`/`focus` на пунктах сайдбара и
  чипах кластеров — к моменту клика чанк уже в кэше.
- CSS: монолит 12 538 строк разрезан на базу `app/globals.css` (5 933 строки) и
  `app/styles/screen-{library,map,chat,settings,vault}.css`, каждый импортирует
  свой экран. Разрез автоматический (`scripts/split-css.py`): правило уходит к
  экрану, только если в селекторе есть класс, который встречается ТОЛЬКО в
  разметке этого экрана; класс, замеченный в каркасе, оставляет правило в базе.
  Каскад сохранён именованными слоями `@layer wfNNN` с объявлением порядка в
  базе, поэтому переопределения работают как до разреза. Источник правды —
  снимок `app/styles/_monolith.css`; проверка — `scripts/check-css-split.py`.
- Резервный конвейер индексатора (`lib/indexer/pipeline` + PDF-парсер) переведён
  на ленивый импорт — в первом бандле его больше нет.
- Инструменты замера: `pnpm bundle:report` (считает то, что реально грузит
  браузер на маршруте: `@next/bundle-analyzer` с Turbopack несовместим),
  `pnpm bundle:analyze` = `next experimental-analyze` для интерактивной карты,
  `ANALYZE=true` включает analyzer для webpack-сборки.

**Замеры маршрута `/` (прод-сборка, до = снимок HEAD, после = текущий код):**

| Метрика | До | После | Δ |
|---|---|---|---|
| Свой JS в первом бандле | 594 kB | 251 kB | **−57,7 %** |
| React/Next runtime (пол, кодом не режется) | 428 kB | 432 kB | +1 % |
| Весь первый JS | 1 023,0 kB | 728,6 kB | −28,8 % |
| Весь первый JS, gzip | 311,3 kB | 228,3 kB | −26,7 % |
| CSS первого запроса | 197,8 kB | 99,5 kB | **−49,7 %** |
| CSS, gzip | 35,4 kB | 19,4 kB | −45,2 % |
| Первый запрос целиком, gzip | 346,7 kB | 247,7 kB | −28,6 % |

Честная оговорка: критерий «первый бандл −40 %» выполнен по коду продукта
(−58 %). По суммарному весу выходит −29 %, потому что 432 kB из 729 kB — это
рантайм React 19 + Next 16 с полифилами; разделение экранов его не режет.

### Гейт этой сессии
`tsc --noEmit` — 0 ошибок; `eslint` — 0 errors (94 warnings, прежний долг
react-hooks); `vitest` — 111 passed (17 файлов: тест ререндеров + тест целостности разреза CSS);
`playwright` — 10 passed / 4 skipped (пропуски: чат с облаком и вход требуют
ключей); все пять экранов открыты и проверены скриншотами.

### Среда
Дисциплина запуска: после `pnpm build` обязателен рестарт воркера
(`pkill -f next-server && node_modules/.bin/next start -p 3000`) — старый
процесс держит манифест прежней сборки и отдаёт 500 на новые чанки, снаружи
это выглядит как «клиент не гидрируется». Supervisor-таск `frontend` всё ещё
описывает CRA-конфигурацию в `/app/frontend` и к этому приложению отношения не
имеет; прод поднимается вручную.
`/app/.env` пересоздан после сброса пода (`APP_PASSWORD`, `APP_SESSION_SECRET`),
`node_modules` восстановлены pnpm, браузер Playwright доустановлен.
Прод-проверка: `pnpm build && node_modules/.bin/next start -p 3000`.

### Дальше (волна 3, фаза 2)
NF-4 онбординг из трёх шагов → LG-3 журнал безопасности → NF-2 локальный движок
(Ollama). Плюс ручная приёмка NF-1 на папке ~1 000 файлов.

---

## Сессия 2026-06-02 · NF-4 «Онбординг из трёх шагов» (волна 3, фаза 2)

**Сделано.**
- `lib/onboarding.ts` — политика первого запуска (кому показывать, что значит
  отказ от ключа, во что превращается выбор режима). Разметка отделена от правил.
- `components/onboarding.tsx` + `app/styles/onboarding.css` — три шага поверх
  каркаса, свой чанк (`next/dynamic` в `app/page.tsx`), в первом бандле его нет.
- Шаг 1: локальный/гибридный с честным перечнем «что уходит»; гибридный требует
  явной галочки, согласие пишется с датой (`cloudConsentAt`).
- Шаг 2: PIN (4–8 цифр) или пароль (8+), повтор обязателен, ключ создаётся через
  `setupLock` (PBKDF2 600 000). Отказ только явный — второй экран с ценой отказа
  и отдельным подтверждением; отказ уходит в ленту событий.
- Отказ от ключа принудительно опускает режим до локального и не выдаёт согласия
  на облако: полудоверенного состояния не остаётся.
- Шаг 3: «Подключить папку» → конвейер NF-1 (`connectFolder`, фолбэк
  `<input webkitdirectory>`), либо «Посмотреть демо».
- Профиль настроек: `settings.onboarding = { at, mode, keyChoice, start }`
  (`finishOnboarding`, `noteOnboarding`, `markOnboarded`). Повторный вход
  онбординг не показывает; профиль со старым замком засчитывается молча;
  перезагрузка посреди онбординга возвращает на третий шаг.

**Гейт этой сессии.**
`tsc --noEmit` — 0 ошибок; `eslint` — 0 errors (101 warning, прежний долг
react-hooks); `vitest` — 135 passed (20 файлов, новый `onboarding.test.ts`);
`pytest tests/api` — 62 passed (по https-адресу превью); `playwright` — 11 passed,
включая новый `11-onboarding.spec.ts`; прод-сборка зелёная, поток проверен в браузере
(гибридный режим доехал до статус-бара, замок встал, онбординг после перезагрузки не
показывается).

**Известное.** Три сценария `03-chat-tool-call` на превью падают на ожидании
`engine-cloud`: после входа чанк экрана настроек иногда не доезжает (вручную тот же
путь проходит). Спека раньше пропускалась целиком, поэтому регрессией это не является;
разбирать отдельно как проблему раздачи чанков (AR-2).

**Среда.** `node_modules` восстановлены (`pnpm@10`: 11-я требует Node 22, стоит 20),
`/app/.env` пересоздан (`APP_PASSWORD`, `APP_SESSION_SECRET`, `AI_PROXY_URL`,
`EMERGENT_LLM_KEY`, `AI_DIR`), браузер Playwright доустановлен в `/pw-browsers`.
API- и e2e-гейты гонять по https-адресу превью: cookie сессии `Secure`.

### Дальше (волна 3, остаток)
LG-3 журнал безопасности → ручная приёмка NF-1 на папке ~1 000 файлов →
узкие подписки для `screen-chat/screen-lock/screen-settings` (P1).

---

## Сессия 2026-06-02 (продолжение) · LG-3 «Журнал безопасности» (волна 3, фаза 2)

**Сделано.**
- Схема IndexedDB поднята до v3: отдельный append-only стор `journal`
  (autoIncrement). Стирание сейфа, очистка ленты уведомлений и retention журнал
  физически не затрагивают.
- `lib/journal.ts` — только `logJournal` / `readJournal` / `subscribeJournal` /
  `journalToFile`. API удаления и очистки нет намеренно; в записи идут факты и
  числа, но не значения полей и не содержимое файлов.
- Точки записи: включение/смена/выключение/сброс замка, отказ от мастер-ключа в
  онбординге, экспорт секретов без шифрования, восстановление бэкапа, стирание
  сейфа, «ИИ сохранил пароль», выдача/отзыв согласия на облако и каждый
  исходящий ход к внешней модели (локальные ходы не логируются).
- Уведомления ведут к факту: новый вид ссылки `{ kind: 'journal', id }`,
  `openNotif` открывает настройки и подсвечивает нужную строку журнала.
- Панель `components/journal-panel.tsx` в настройках: счётчик, фильтр по реально
  случившимся типам, выгрузка в JSON, красная риска у необратимых событий,
  прямая оговорка «журнал не чистится».
- Побочно починено: онбординг больше не оставляет ложный флаг «есть
  несохранённые изменения» в настройках (черновик синхронизируется).

**Гейт.** `tsc` 0 ошибок; `eslint` 0 errors (102 warning — прежний долг);
`vitest` 141 passed (новый `journal.test.ts`, `db.test.ts` обновлён на схему v3);
`pytest tests/api` 62 passed; `playwright` 12 passed, включая новый
`12-journal.spec.ts` (спека чата исключена — известный флейк раздачи чанков);
прод-сборка зелёная, поток проверен в браузере.

### Дальше
Ручная приёмка NF-1 на папке ~1 000 файлов → узкие подписки для
`screen-chat/screen-lock/screen-settings` (P1) → флейк `03-chat-tool-call`.

---

## Сессия 2026-06-02 (хвост) · Волна 3 закрыта

**1. Баг «экран настроек иногда не доезжает после входа» — исправлен.**
Причина: клик по навигации уходил в ещё не гидратированную кнопку (HTML от
сервера есть, обработчиков нет), клик пропадал молча, а hover-подсвет в сайдбаре
создавал иллюзию перехода. Сделано: каркас публикует `data-app-ready` на
`data-testid="app-shell"`; e2e ждут его хелпером `waitAppReady`
(`tests/e2e/ready.ts`); загрузка чанков экранов повторяется дважды
(`withRetry` в `components/screens.tsx`), окончательная ошибка доходит до
boundary с карточкой «Экран не открылся». Три чат-сценария снова зелёные.

**2. Узкие подписки для чата, замка и настроек.** `screen-lock` → `useLockStore`;
`screen-settings` → настройки + данные + уведомления + навигация + тост + движок;
`screen-chat` → композиция `data`/`settings`/`nav` плюс `notify` и `flash`.
`useVault()` остался только у каркаса — все пять экранов сняты с фасада.

**3. Тревожные события журнала.** `isSevereKind()` в `lib/journal.ts`; плашка
«необратимо» в строке, фильтр «Только необратимое · N» (появляется лишь когда
такие записи есть) и метка в статус-баре `components/journal-alert.tsx`
(«ЖУРНАЛ · N НЕОБРАТИМЫХ»), ведущая прямо в раздел журнала с любого экрана.

**Гейт.** `tsc` 0 ошибок; `eslint` 0 errors (104 warning — прежний долг);
`vitest` 141 passed; `playwright` 16/16 passed (включая три чат-сценария и
второй сценарий миграции, которые раньше падали или пропускались);
`pytest tests/api` 62 passed; прод-сборка зелёная.
Независимая приёмка — `test_reports/iteration_16.json`: критических и минорных
проблем нет, 8/8 холодных входов открывают настройки, стресс-сценарий
`tests/e2e/99-settings-nav-stress.spec.ts` 9/9.

**Статус волны 3: ВЫПОЛНЕНА (2026-06-02).** Отмечено в
`docs/tasks/README.md`, `docs/tasks/wave-3.md`, `docs/tasks/wave-3-handoff.md`.
Единственный открытый пункт волны — не код, а ручная приёмка NF-1 на реальной
папке ~1 000 файлов.

### Дальше
Волна 4 «Зрелость и рост» (`docs/tasks/wave-4.md`): NF-3 центр активности,
UX-3 фильтры панели событий, UX-5 явный демо-режим и остальное.
Ограничение для CI: `/ai-api/chat` — 10 ходов в минуту, чат-набор нельзя гонять
с `--repeat-each` ≥ 5.


---

## Сессия 2026-06-02 · Волна 4 старт — NF-3 «Центр активности» — DONE

**Решения владельца (ask_human):** полный вариант (живая полоса «Сейчас» +
единая лента истории); пункт сайдбара в секции «Система» рядом с «Настройки»;
журнал остаётся отдельной панелью в Настройках, а в Центре активности агрегируется.

**Сделано.**
- Новый экран `activity`: `components/screen-activity.tsx` + `app/styles/screen-activity.css`
  (свой ленивый чанк, в первом бандле его нет). `ScreenId 'activity'` в `lib/store/nav.tsx`,
  чанк в `components/screens.tsx`, пункт «Центр активности» в секции «Система» над
  «Настройки» + PLACEHOLDER/workspaceCount/statusText в `components/app-shell.tsx`.
- Живая полоса «Сейчас» (`LiveNow`): 4 плитки реального состояния — индексация с прогрессом
  (единственный подписчик частого прогресса индексатора, чтобы лента не дёргалась), движок ИИ,
  исходящий трафик, замок; пилюля со счётчиком фоновых процессов, пульс у активных.
- Единая лента истории: журнал безопасности (LG-3) + активные уведомления в одном потоке,
  свежее сверху, бейдж источника, относительное время. Фильтры: тип (чипы Все / Тревожное · N /
  категории), период (дропдаун За всё время / Сегодня / За 7 дней), объект (дропдаун по типам
  ссылок). Клик по строке ведёт к источнику (уведомление → openNotif; запись журнала →
  Настройки с подсветкой строки journal:<id>). Выгрузка ленты в JSON.
- Журнал по-прежнему живёт панелью в Настройках — в Центре активности агрегируется, а не
  переезжает. В `components/dropdown.tsx` добавлен опциональный проп `testId` на триггер.

**Метрики системы (по запросу владельца).** Секция «Метрики системы» между «Сейчас» и лентой:
собственные SVG-графики (zero-dependency, как QR-кодер), только честные локальные измерения —
никаких выдуманных серверных метрик. Четыре карточки:
- **Нагрузка · память** — живой area-спарклайн JS-кучи вкладки (`performance.memory`, замер раз в
  2 с, последние 60 точек), % лимита и число ядер; фолбэк, если браузер не отдаёт heap.
- **Хранилище** — `navigator.storage.estimate()`: занято/квота + полоса и крупный процент.
- **Исходящий трафик · 24 ч** — часовые столбики облачных ходов из журнала (`cloud-request`),
  флагман приватности: «наружу ничего не ушло» при нуле (зелёный), предупреждающий тон при >0.
- **Активность · 24 ч** — часовые столбики всех событий ленты за сутки.
data-testid: `activity-metrics`, `metric-memory|storage|cloud|events`. SVG-компоненты
`AreaSpark`/`BarSpark` в `screen-activity.tsx`, стили — слой «метрики» в `screen-activity.css`.

**Доработки метрик (по запросу владельца).**
- **Живой облачный счётчик:** плитка «Исходящий трафик» в полосе «Сейчас» (`LiveNow`) теперь
  показывает число облачных ходов за сегодня прямо из журнала (`cloud-request`, с местной
  полуночи), тон предупреждающий при >0, `data-testid=activity-cloud-today`. Подписка на
  `subscribeJournal` — счётчик живой и переживает перезагрузку.
- **История нагрузки между сессиями:** график памяти персистится в `localStorage`
  (`wf.metrics.heap.v1`, окно 6 ч, коарс-семпл раз в 30 с, cap 500) — всплески видны за часы,
  а не только в текущей сессии. Данные не чувствительны (только размеры JS-кучи). Проверено:
  42 сэмпла грузятся, спарклайн рисует линию из 41 сегмента.

**Гейт.** `tsc --noEmit` 0 ошибок; прод-сборка зелёная (`next build`), supervisor `frontend`
теперь честно поднимает прод (`next start`) — лончер `/app/frontend/package.json` починен
(указывал на несуществующий `next/dist/bin/next` при pnpm-раскладке). Независимая приёмка
`test_reports/iteration_17.json` — 100% (новый `tests/e2e/13-activity-center.spec.ts`,
регрессия `12-journal.spec.ts`), багов нет.

**Известное.** Внешний preview-URL периодически показывает платформенный splash
«Preview Unavailable» (под «отдыхает») — будится пользователем; приложение при этом работает
на `localhost:3000`. `components/screen-activity.tsx` ~600 строк — при расширении вынести
`LiveNow` в отдельный файл.

## Волна 4 · NF-5 Bulk actions + NF-6 Командный центр (2026-09-02)

**NF-5.** Массовые операции живут в `lib/bulk.ts` (порции по 25, поток отдаётся интерфейсу
между порциями, прогресс из настоящего счётчика, отмена на границе порции, окно возврата
10 секунд) и в общей панели `components/bulk-bar.tsx` — одна на библиотеку и менеджер
секретов. В библиотеке: Ctrl/Cmd+клик, Shift+клик диапазоном, кнопка «Выделение»,
«выбрать всё в фильтре» (весь фильтр, а не страница доски), действия — метка,
кластер, файловый ключ на группу, удаление с возвратом. В секретах: метка, папка,
избранное, корзина; в корзине — восстановление и безвозвратное удаление. Сторы получили
порционные операции: одна запись в хранилище на порцию, а не на объект.
Критерий закрыт: на корпусе из 500 файлов выбор даёт 504 объекта, прогресс идёт,
интерфейс отвечает во время операции, отмена останавливает на 400 из 500, «Вернуть»
откатывает применённое.

**NF-6.** `lib/commands.ts` — реестр из 33 команд (20 действий, 6 переходов, 7 настроек)
с подсказками и доступностью по контексту; команды, живущие внутри экрана, работают
из любого места через «намерения» (экран подхватывает заявку при монтировании).
Палитра Ctrl+K осталась поиском по сейфу — сущности остаются первой группой, дальше
группы команд и «Недавнее» (`wf.commands.recent.v1`). Курсор ходит только по
запускаемым строкам; недоступная команда видна с причиной отказа.

**Гейт.** `tsc --noEmit` 0 ошибок, `next build` зелёная, unit 162 теста (новые
`tests/unit/bulk.test.ts`, `tests/unit/commands.test.ts`), e2e — весь каталог
зелёный (34 passed, 0 failed), включая новые `14-bulk-and-commands.spec.ts`
(7 сценариев) и независимую приёмку `15-bulk-commands-review.spec.ts`
(5 сценариев). Отчёт — `test_reports/iteration_18.json`.

**Исправлено на приёмке.** «Выбрать всё в фильтре» выделяло только нарисованную
страницу доски; сброс состояния по `lockEpoch` гасил модалку, открытую командой
палитры; Esc и первые буквы после Ctrl+K терялись из-за паузы фокуса, а Esc со
стрелками не работали вне поля ввода.

**Попутно.** `app/styles/screen-activity.css` переведён в объявленный слой `wf047`
(последний в порядке — приоритет сохранён), и тест разреза CSS снова видит все слои;
новый слой `wf046` — стили панели массовых действий и командного центра.
Восстановлен `/app/.env` (пароль входа из `memory/test_credentials.md`).

**Независимая приёмка NF-5/NF-6 (2026-06, `test_reports/iteration_19.json`).**
Внешний тестовый агент прогнал только фронтенд-флоу обеих задач в браузере на
preview: оба критерия — PASS, баги не найдены, регрессов по четырём ранее
исправленным дефектам нет. 504 объекта выбираются целиком, прогресс живой
(50 → 250 → 500 из 504), UI отвечает во время операции, отмена честная (250 из 500
по сверке с IndexedDB), Undo откатывает. Палитра: 33 команды, Ctrl+K с любого
экрана, клавиатура без потерь, недоступные команды помечены с причиной,
«Недавнее» переживает перезагрузку.

Остались три необязательных UX-замечания (LOW), в код не вносились по решению
пользователя: очищать/предвыделять строку запроса при открытии палитры (иначе
«Недавнее» скрыто старым запросом); подтверждение для массового удаления сотен
объектов; согласовать «504» в счётчике выбора и «из 500» в прогрессе действий,
применимых только к файлам.

### Дальше (Волна 4)
UX-3 фильтры панели событий · UX-5 явный демо-режим · UX-4 accessibility ·
LG-4/LG-5 · RM-3.

## Волна 4 · NF-7 бэкап всего сейфа и NF-8 автономный режим (2026-09-03)

**NF-7.** Появился `lib/backup/*`: один зашифрованный снимок всех модулей сейфа
под ОТДЕЛЬНЫМ паролем (`sealPortable`: PBKDF2 600 000 → AES-GCM-256), ротация,
клиентское расписание (день/неделя) и восстановление с превью состава и выбором
модулей. Снимок собирается на уровне документов хранилища (семь модулей:
секреты, библиотека и стикеры, разговоры, индекс содержимого, настройки, лента
событий, журнал), поэтому восстановление даёт идентичное состояние. Мастер-ключ
и состояние замка в снимок не входят; вместо этого внутрь шифртекста уезжает
сырьё ключей (SEK, файловые ключи, секреты locked-стикеров), и при восстановлении
оно заворачивается под мастер-ключ принимающего устройства — снимок оживает на
чистом устройстве с ДРУГИМ мастером. Журнал безопасности только дополняется:
append-only лента не заменяется никогда. Интерфейс — раздел «Бэкап сейфа» в
настройках, файл выгружается как `.vaultbak`.

**NF-8.** `lib/flags.ts` — флаги `dev`, `experimental`, `mcp.skeleton` в
`wf.flags.v1` (localStorage, синхронное чтение) переживают перезагрузку и вторую
вкладку. `lib/net.ts` — обёртка над `fetch`, `XMLHttpRequest`, `WebSocket`,
`EventSource` и `sendBeacon`: в автономном режиме запрещены чужой origin и свои
маршруты, которые ходят наружу за клиента (`/proxy/*`, облачный ход
`/ai-api/chat`), локальная работа не трогается. Каждый запрет попадает в счётчик:
статус-бар показывает «АВТОНОМНЫЙ РЕЖИМ · N ЗАПРЕТОВ», раздел настроек — последние
попытки с адресом и способом. Флаги влияют на продукт по-настоящему: `dev` — строка
диагностики в статус-баре, `experimental` — режим «слияние» при восстановлении,
`mcp.skeleton` — чтение списка MCP-серверов из локальной папки `ai/`.

**Гейт.** `tsc --noEmit` 0 ошибок, `next build` зелёная, unit 178 тестов (новые
`tests/unit/backup.test.ts` — буквальный round-trip на «чистом устройстве» с другим
мастер-ключом, и `tests/unit/net-flags.test.ts`), e2e — весь каталог зелёный
(37 passed, 4 skipped), включая новый `tests/e2e/16-backup-flags.spec.ts`.

**Исправлено на приёмке (`test_reports/iteration_20.json`).** Независимая
браузерная проверка нашла главное: на чистом профиле демо-набор первого запуска
живёт в сторе на значениях по умолчанию, документа `wf.files.v1` в IndexedDB ещё
нет — и снимок его не включал, поэтому «стереть сейф → восстановить» терял то,
что человек видел на экране. Теперь снимок собирается и из живого состояния
(`lib/backup/live.ts`), а третий e2e-сценарий проверяет именно этот путь.
Попутно: кнопка «Войти» больше не блокируется, когда автозаполнение опережает
гидратацию (React-state расходился с DOM); блок MCP при 401 честно говорит
«нужен вход в приложение»; счётчик запретов склоняется по-русски; узкие окна
(≈768 px) получили раскладку в один столбец, подпись способа в журнале запретов
стала читаемой. Не тронуты два замечания LOW: подсветка активного пункта рельса
настроек по прокрутке и общий адаптив каркаса на планшете — это поведение
каркаса, а не новых разделов.

**Попутно.** Восстановлен `/app/.env` (пароль входа из
`memory/test_credentials.md`, новый локальный `APP_SESSION_SECRET`) и установлены
зависимости: pnpm 11 требует Node ≥ 22.13, в поде стоял Node 20 — поставлен
Node 22 (arm64).

## Волна 4 · UX-3 фильтры панели событий (2026-09-03)

Выбранный фильтр панели событий живёт в `wf.notifs.filter` и переживает
закрытие панели и перезагрузку (значение проверяется `parseNotifFilter`, мусор
превращается в «Все»). Главное — счётчики: теперь и бейдж колокольчика, и чипы
фильтров считает одна функция `unreadCount(all, now)`, которая исключает архив
И отложенные. Раньше бейдж считал отложенные, а панель их скрывала — цифра
обещала события, которых в списке нет. Отложенные показаны отдельной строкой
подвала («Отложено: N — вернутся в ленту сами») и возвращаются сами по грубому
тику. Выключенная категория помечена точкой с настоящей подсказкой
(`role="img"` + `aria-label`, а не невидимый для читалки `title`). Компактный
вид ≤ 420 px: панель крепится к экрану, а не к колокольчику (раньше на 380 px
уезжала за левый край на 36 px), подписи фильтров короткие, полоса чипов
прокручивается одной строкой.

**Гейт.** `tsc --noEmit` 0 ошибок, `next build` зелёная, unit 181 тест
(+3 в `tests/unit/notifs.test.ts`), e2e весь каталог 41 passed, включая новый
`tests/e2e/17-notifs-filters.spec.ts` (4 сценария).

### Дальше (Волна 4)
UX-5 явный демо-режим · UX-4 accessibility · LG-4/LG-5 · RM-3.

## Волна 4 · Четыре бага UI (2026-09-03)

**#2 (P0) Создание PIN/пароля.** Экран первого запуска расходился с настройками
и по дизайну, и по правилам: простые `<input type="password">` и `maxLength=8`
при `validateSecret` `/^\d{4,8}$/`, а экран разблокировки жёстко требует 6 цифр —
PIN из 4–5 цифр создавался, но сейф им не открывался. Теперь единый источник
правды: `lib/lock-store.ts` → `/^\d{6}$/` («PIN: ровно 6 цифр»),
`security-section.tsx` `onCreate` тоже `/^\d{6}$/`. Поля вынесены в общий
`components/mk-fields.tsx` (`MkPinRow` с `PIN_LENGTH = 6`, `MkPassField`,
`strengthPw`) и переиспользованы в настройках и в онбординге — шесть ячеек,
счётчик n/6, «глаз», индикатор силы. Кнопка «Создать» активна только при
шести совпадающих цифрах (или пароле 8+ с совпадающим повтором). Правила
`.mk-*` перенесены в базовый слой `app/globals.css` — онбординг живёт вне
ленивого чанка настроек.

**#1 (P0) Настройки листались в пустоту.** Корень — не высота `.set-page`, а
скрытый `.sr-only` file input в разделе «Бэкап сейфа»: без позиционированного
предка он считался от `html`, стоял на y≈4351 и растягивал страницу на 3552 px
пустоты. `.scroll-col` получил `position: relative` (лечит весь класс таких
багов на любых колонках прокрутки), `.set-page` переведён с `height:100%` на
`flex:1; min-height:0` — «сто процентов» не вычитали топбар. Внешний скролл
страницы теперь 0 на всех экранах.

**#3 (P1) Фон карты.** Оба блока `.cos-deep` в монолите затемнены, альфы
радиальных пятен снижены, плюс блок «MAP v2.4» в конце базы гасит то, что
переигрывало градиент: кадры анимаций туманностей (они задают opacity),
звёздные слои и виньетку. `.map-space` — `#000102`.

**#4 (P1) Лого на логине.** На `/login` вместо текстового `<h1>WorkSpaceX</h1>` —
`IconLogoMark` в знаке и `LogoWord` (`WORKFLO`+акцентная `W`) внутри
`h1[data-testid="login-logo"]`, стиль `.login-logo`.

**Гейт.** `tsc --noEmit` 0 ошибок, `next build` зелёная, unit 181 тест
(включая `tests/unit/css-split.test.ts`), independent frontend-приёмка —
`test_reports/iteration_21.json`: 4/4 бага FIXED, регрессов нет.
`tests/e2e/11-onboarding.spec.ts` обновлён под шесть ячеек PIN.

**Внимание про CSS.** Сгенерированные `app/globals.css` и
`app/styles/screen-*.css` расходятся со `scripts/split-css.py` (в скрипте нет
экрана activity и последних блоков монолита), поэтому `--write` затирает
`screen-activity.css` и ломает `tests/unit/css-split.test.ts`. До синхронизации
скрипта правку вносим и в `_monolith.css`, и зеркально в сгенерированный файл.

### Дальше (Волна 4)
Синхронизировать `split-css.py` с фактическим CSS · UX-5 явный демо-режим ·
UX-4 accessibility · LG-4/LG-5 · RM-3.

## Волна 4 · Поля записи в менеджере секретов (2026-09-03)

Правая колонка действий (`.vte-acts`) жила под `opacity: 0` и проявлялась только
при наведении или у зашифрованного поля — справа висела мёртвая полоса, а кнопки
выпрыгивали. Колонка убрана: замок (`.vte-lock`, акцентный во включённом
состоянии) и удаление (`.vte-del`) переехали внутрь поля значения к «глазу»,
генератору и копированию; тип поля остался узким чипом (`.vte-kind`, 84 px,
рамка волосяная, подсвечивается при наведении на строку). Подпись «замок —
шифруется» сохранена. Ярлык типа `multiline` сокращён до «абзац», чтобы чип не
обрезался. Проверено вручную: переключение замка, индикатор силы пароля и
удаление поля работают; `tsc` 0 ошибок, unit 181, `next build` зелёная.

## Итерация 23 (2026-06) · закрытие замечаний ревью iter_21/22
- `components/screen-lock.tsx`: ячейкам PIN добавлен `data-testid="lock-cell-{i}"`
  (хрупкий селектор `.lock-cells .lock-cell` больше не нужен).
- `components/screen-settings.tsx`: «Опасная зона» получила `settings-clear-index`,
  `settings-delete-vault`, `settings-delete-vault-confirm/-cancel`.
- `components/security-section.tsx`: submit в окне «Выключить замок» переименован
  в «Подтвердить» — текст больше не совпадает с триггером `mk-disable-open`.
- Восстановлена среда пода: pnpm 10 + yarn, `/app/.env`, прод-сборка Next.
- Проверено скриншотами: вход → онбординг → Ctrl+Shift+L → разблокировка через
  `lock-cell-*` → «Настройки» → «Опасная зона» с новыми testid.

## Итерация 23b · уборка файловой системы (2026-06)
- Удалены артефакты, не относящиеся к проекту: `.screenshots/07_corrupt.png`
  (случайно закоммиченный скриншот прогона), `test-results/` (вывод Playwright,
  был в git несмотря на .gitignore), `tsconfig.tsbuildinfo`, локальный `.pnpm-store/`.
  В `.gitignore` добавлен `.screenshots/`.
- `test_reports/`: iteration_1..18 → `test_reports/archive/`, в корне каталога —
  три последних отчёта + новый `README.md` с правилами хранения.
  Восстановлен потерянный `iteration_22.json`, добавлен `iteration_23.json`.
- Проверено, что мусора «по существу» нет: скан app/components/lib/hooks на
  неиспользуемые модули дал 0 сирот; `app/styles/_monolith.css` — источник
  правды для `scripts/css:split` (удалять нельзя), `backend/` и `frontend/` —
  обязательные заглушки для supervisor, дубль PDF в `public/` и `docs/audit/`
  задокументирован как намеренный.
- Регресс: `npx vitest run` — 24 файла / 181 тест PASS, `/login` → 200.
- Размер репозитория без node_modules/.next: 7.5 МБ (было ~21 МБ).

## Итерация 24 · оптимизация (2026-06)
Замер до/после (`pnpm bundle:report`, прод-сборка):

| Маршрут | Было (gzip) | Стало (gzip) |
|---|---|---|
| `/`      | 257.2 kB | 256.3 kB |
| `/login` | 197.1 kB | 196.0 kB |

Браузерные метрики (preview, десктоп): FCP 216–232 мс, переключение экранов
**было** 25–608 мс (холодный чанк чата 608 мс) → **стало** 25–31 мс для
прогретых, задержка ввода в поиске ~10 мс/символ, длинных задач при прокрутке нет.

Что сделано (точечно, без переделки архитектуры):
- `@vercel/analytics` убран из корневого layout и из зависимостей: вне Vercel
  скрипт всё равно давал 404, а его чанк висел в первом бандле каждого маршрута.
- `tw-animate-css` удалён — пакет не использовался ни в одном классе.
- `components/app-shell.tsx`: чанки всех экранов догружаются в
  `requestIdleCallback` после первого кадра. Клик с клавиатуры/из палитры больше
  не ждёт сеть (чат: 608 мс → 31 мс).
- `lib/store/nav.tsx`: выдача поиска считается на `useDeferredValue(query)` —
  строка перерисовывается сразу, проход по корпусу не блокирует ввод.
  Инвариант рендеров в `tests/unit/store-renders.test.tsx` обновлён на 2 прохода.
- `lib/db/persist.ts`: записи в IndexedDB на один ключ склеиваются — серия правок
  (драг стикера, набор в редакторе) даёт одну транзакцию с самым свежим
  значением вместо десятков.
- Новый инструмент замера: `scripts/chunk-sizes.mjs` (`pnpm bundle:chunks [порт]`)
  — показывает, какие ассеты `/_next/static` реально грузит маршрут.

Где пол по весу (не трогаем без отдельной задачи):
- `/login`: 572 kB JS — это react-dom + рантайм App Router; своего кода 20 kB.
- База CSS 100 kB (21 kB gzip) грузится на всех маршрутах, включая `/login`.
  Вынести «оболочку сейфа» из `globals.css` можно только через `scripts/split-css.py`
  (P2, потребует правки инструмента и `tests/unit/css-split.test.ts`).
- Шрифты: 5 файлов woff2 (~89 kB, latin+cyrillic × 3 веса Sans + 2 Mono) —
  все веса реально используются в дизайн-системе.

Регресс: `npx tsc --noEmit` чист, `npx vitest run` 24/181 PASS, `npx eslint .`
0 ошибок, `/` и `/login` отвечают 200.

## Итерация 24 · WorkSpaceX: масштаб, лого, сплэш, фикс карты (2026-06)
**Ренейм.** Продукт называется **WorkSpaceX**: заголовок вкладки, сайдбар
(WORKSPACEX / local ai workspace), экраны входа и блокировки, онбординг, узел
«WORKSPACEX CORE» на карте, тексты в UI, README и вся проза в `docs/`.
Технические идентификаторы НЕ переименованы намеренно (совместимость данных):
имя базы IndexedDB `workflow`, ключи `wf.*`, `AI_DIR=/root/.workflow/ai`,
`kind: workflow-secrets-plain` в снимках, имена выгрузок `workflow-journal-*`.

**Лого.** `IconLogoMark` — рамка рабочего пространства и литера «X» как граф из
четырёх узлов и двух связей, ядро акцентное. `LogoWord` = WORKSPACE + акцентная X.

**Масштаб интерфейса (Настройки → «Интерфейс»).** `lib/ui-scale.ts` держит
множитель 80–150% шагом 5% в `localStorage['wf.ui.scale']`, применяет через
`--ui-zoom` + `body { zoom }`. Bootstrap-скрипт в `app/layout.tsx` поднимает
значение до первого кадра (нет мигания). Горячие клавиши Ctrl + / Ctrl − / Ctrl 0
в `app-shell`. Каркас держит высоту окна как `calc(100vh / var(--ui-zoom))`,
сайдбар прокручивается сам — внешнего скролла страницы нет ни на одном масштабе.

**Сплэш и скелетоны.** `components/app-splash.tsx` — знак собирается из линий,
слово проявляется, полоса бежит; уходит по `ready && hydrated`, страховка 2.5 с,
повторно за сессию не появляется. `ScreenLoading` в `components/screens.tsx` —
скелетон-раскладка вместо строки текста.

**Фикс лагов «Инспектора узла» (жалоба пользователя).** Настоящая причина была
не в панели, а в канвасе карты: экран шёл ~5 к/с даже в покое, прокрутка просто
наследовала этот бюджет. Сделано:
- `getContext('2d', { alpha: false })` + заливка фона вместо `clearRect` —
  браузер больше не смешивает канвас со страницей каждый кадр;
- адаптивный бюджет кадра: если отрисовка дороже 12 мс, карта честно идёт 30 к/с;
- `dpr` ограничен 1.5 вместо 2 (−44% пикселей);
- `onMove` не читает `getBoundingClientRect`, когда курсор не над канвасом
  (прокрутка панели больше не вызывает forced layout);
- CSS: плотный фон панели, `contain: paint` и свой слой у `.ni-scroll`.
Замер: карта в покое 186 мс/кадр → **17 мс**, прокрутка инспектора 188 → **17 мс**.

**Приватность (замечание тестировщика, исправлено).** Черновик поля-ссылки
(«val0») проходил как домен и уходил в `google.com/s2/favicons`. Теперь
`domainOf` требует настоящее доменное имя, а прямой запрос к Google из браузера
убран — иконки идут только через свой `/proxy/favicon`.

Приёмка: `/app/test_reports/iteration_24.json` — 5/5 блоков PASS, action_items нет.
Регресс: `tsc --noEmit` чист, `vitest` 181/181, `eslint` 0 ошибок.

---

## UX-4 · Доступность панелей и модалок (2026-06, сделано)

**Общий хук `hooks/use-dialog.ts`.** Одна реализация на все диалоги:
- `useDialog({ onClose, label, open, modal, initialFocus, autoFocus })` —
  `role="dialog"`, `aria-modal`, ловушка фокуса по Tab/Shift+Tab, Escape и
  возврат фокуса на элемент, с которого диалог открыли. Стек открытых диалогов
  модульного уровня: Escape обслуживает только верхний, вложенные не схлопываются.
- `useListNav({ selector, orientation, loop })` — стрелочная навигация по
  списку через DOM-фокус (`data-nav-item`), Home/End, зацикливание.
- `components/dialog-shell.tsx` — обёртка для разметки, собираемой в выражении
  (модалки файлового ключа в библиотеке), где своих хуков нет.

**Подключено.** `components/notifications.tsx` (панель = диалог, стрелки водят
по ленте из любого места панели, фильтры — горизонтальные стрелки, роль tablist
с roving tabindex), `components/dropdown.tsx` (`aria-activedescendant`,
`aria-controls`, id у опций), модалки `components/vault/*` (генератор, QR Wi-Fi,
редактор записи, импорт/экспорт), обе модалки файлового ключа в библиотеке.

**Контраст и 768 px.** Неактивные элементы больше не гасятся `opacity` —
им выдан токен `--text-off: #99a3ad` (≈7:1 на панели). Токен мелких подписей
`--text-3` поднят с `#565f69` (3.0:1) до `#7b848e`; пульс таймера стикера
не проваливается ниже порога. На 768 px панель уведомлений крепится к экрану,
подсказки переносятся, а не режутся многоточием.

**Axe.** Библиотека и сейф: критичных и серьёзных нарушений нет.
Исправлены `label` (скрытый file input без имени), `nested-interactive` и
`aria-allowed-role` (карточка больше не `role="button"`, но остаётся в обходе
с клавиатуры и открывается Enter/Пробелом), `heading-order` (секции — `h2`),
`landmark-one-main` на пяти экранах. Осталось в бэклоге: `region` и
дублирующиеся `banner/contentinfo` на экране настроек (moderate).

## UX-5 · Явный демо-режим (2026-06, сделано)

**`lib/demo-seed.ts`** — весь показательный корпус (13 файлов, 4 стикера,
1 разговор), каждый объект помечен `demo: true`. Модуль вынесен из основного
бандла: `lib/store/data.tsx` тянет его динамическим `import()` и только когда
сейф пуст и демо ещё не отключали. Из `lib/data.ts` убраны `VAULT_FILES`,
`VAULT_VIEWS`, `fileById`, `viewById`, `VAULT_STATS`; из `lib/notes.ts` —
`seedNotes`; из `lib/chat-data.ts` — `seedSession`.

**Состояние.** `localStorage['wf.demo.v1'] = { seeded, dismissed }`.
Профиль со старой сборки: демо-объекты помечаются по id при первом запуске,
поэтому «Начать с чистого сейфа» находит, что убирать.
Если в сейфе уже лежат чужие данные — демо не подмешивается вовсе.

**UI.** Баннер «В сейфе показательные данные» в библиотеке с кнопкой
«Начать с чистого сейфа» (убирает только `demo`-объекты, пользовательские
не трогает), плашка «демо» на карточках файлов и стикеров, метка «ДЕМО N»
в топбаре. Тест-иды: `demo-banner`, `demo-clear`, `demo-pill`, `demo-tag-{id}`.

**Регресс.** `tsc --noEmit` чист, `vitest` 181/181, `eslint` 0 ошибок,
`playwright` 42/45 — три падения среды и до правок: два сценария чата требуют
облачного движка, `06-storage-migration` (вход через `/login`) падает и на
исходном коде. Обновлены ожидания в `14`/`15` (500 файлов → демо не сеется,
504 → 500) и в `16` (демо теперь ложится в базу, а не живёт значением по умолчанию).

### Бэклог
- P2: `region` и дублирующиеся landmark-и на экране настроек (axe, moderate).
- P2: `06-storage-migration` — миграция при входе через `/login` (падало и раньше).
- P2: демо-разговор в рельсе чата помечен `demo`, но отдельной плашки в списке
  разговоров пока нет.

---

## LG-4 · Digest как контейнер (2026-06, сделано)

Сводка конвейера стала настоящим контейнером, а не строкой с числом.
- Раскрывается прямо в карточке: каждое склеенное событие — своя запись со
  своим временем, пометкой «новое» и переходом к источнику
  (`openNotif(digestId, itemId)` → `readNotifItem` в `lib/store/notifs.tsx`).
- `unread` не теряется: непрочитанность контейнера — производная от состава
  (`digestUnread` в `lib/notifs.ts`). Прочитать сводку целиком = прочитать
  состав; прочитать одно событие = прочитать только его.
- Склейка только в живую сводку (`isVisible`) — раньше события могли уехать
  в архивированную запись; добавлен `pruneNotifs` и потолок в 50 событий.
- Плюрализация заголовка (`plural`, `digestTitle`), метка «есть новые»
  в шапке и счётчик новых у кнопки раскрытия.
- Подсказка про выключенную категорию: «новые события не придут, накопленные
  остаются» — и в тосте выключения, и в подвале панели.

## LG-5 · Идемпотентность мутаций (2026-06, сделано)

- `lib/exclusive.ts` — сторож вне React: `run(id, fn, { rollback, dedupMs })`
  возвращает `{ ok }` либо `busy` / `duplicate` / `error`. Дедуп синхронный,
  потому что второй клик приходит раньше рендера.
- `lib/store/mutations.tsx` + фасад `useVault().runExclusive / isPending`.
- Применено: приём файлов, подключение папки, переиндексация (библиотека и
  настройки), импорт секретов (`repeated` вместо задвоения), скилл
  `save_password` (ключ по содержимому, `dedupMs` 5 минут).
- Кнопки в работе гаснут и показывают «Принимаю…» / «Подключаю…» / «Читаю…».

**Регресс на 2026-06-03.** `tsc` чист, `vitest` 190/190, `eslint` 0 ошибок,
`playwright` 44/47 — три падения средовые и воспроизводятся на исходном коде
(два сценария чата требуют облачного движка, `06-storage-migration` через
`/login`). Новый спек: `tests/e2e/18-digest-and-idempotency.spec.ts`.

### Бэклог после волны 4
- P2: RM-3 · скелет MCP как макет — единственная незакрытая задача волны.
- P2: `region` и дублирующиеся landmark-и на экране настроек (axe, moderate).
- P2: `06-storage-migration` — миграция при входе через `/login`.

---

## RM-3 · Скелет MCP помечен как макет (2026-06, сделано)

- `app/ai-api/mcp/[id]/route.ts`: убраны выдуманная задержка и правдоподобная
  ссылка на notion.so (теперь `mock://notion/<запрос>`), убрано поле `updated`.
  Каждый ответ несёт `mock: true` и `notice: 'макет, не реальные данные'`.
- `components/chat/tool-card.tsx`: жёлтая плашка «макет, не реальные данные»
  перед результатом скилла; признак едет через `ExecResult.mock` → `ToolRun.mock`.
- Флаг `mcp.skeleton` прячет вкладку «MCP-серверы» в AI-центре и выключает
  скилл `notion_pull` (он честно сообщает модели, что источника нет).
  Сервер не предлагает `notion_pull`, пока MCP-сервер выключен в `ai/mcp/`.
- Тесты: `tests/unit/mcp-skeleton.test.ts`, `tests/e2e/19-mcp-skeleton.spec.ts`.

**Волна 4 закрыта полностью.** `vitest` 193/193, `eslint` 0 ошибок.

---

## NF-9 · Телеметрия по согласию (2026-06, сделано)

- `lib/telemetry.ts`: локальная агрегация счётчиков. Имена событий — из закрытого
  словаря, всё остальное отбрасывается, поэтому содержимое (имена файлов, текст
  запросов) физически не может попасть в телеметрию. Идентификатора устройства нет.
- Считаются экраны, ключевые действия (приём файлов, переиндексация, поиск, ход
  чата, разблокировка, создание записи в сейфе, экспорт бэкапа, очистка демо)
  и обрывы сценариев (оборванный/упавший ход чата, неудачный приём файлов,
  неверный ключ, пустой поиск).
- Настройки → Приватность: панель «Что будет отправлено» показывает сам payload
  (`telemetry-payload`), счётчики по трём осям, кнопки «Отправить сейчас»
  и «Очистить накопленное». По умолчанию согласия нет — отправка недоступна.
  Отзыв согласия стирает накопленное немедленно.
- Приём: `POST /ai-api/telemetry/usage` → `ai/telemetry/*.json` (в .gitignore).
  Сервер принимает только известные ключи и конечные числа; маршрут за сессией
  и лимитом частоты. Успешная отправка обнуляет счётчики.
- Тесты: `tests/unit/telemetry.test.ts`, `tests/e2e/20-telemetry-consent.spec.ts`.

---

## NF-10 · MCP-сервер наружу (2026-06-04, сделано)

- `POST /mcp` — MCP Streamable HTTP (JSON-RPC 2.0, zero-dependency): `initialize`,
  `ping`, `tools/list`, `tools/call`. Только Bearer-токен `wsx_<id>_<secret>`,
  сессия браузера не принимается; `GET /mcp` → 405.
- `lib/permissions.ts`: области `search` / `read` / `notes:write` / `secrets:write`;
  инструменты `search`, `list_files`, `get_metadata`, `create_sticker`, `create_secret`.
  Значения секретов, тексты файлов и тела стикеров наружу не уходят.
- `lib/mcp-server.ts`: токены (хеш SHA-256 в `AI_DIR/mcp-access/tokens.json`, срок,
  отзыв, счётчик вызовов), append-only аудит `audit.jsonl`, очередь заданий и
  ожидающие подтверждения в памяти. Лимит 60 вызовов/мин на токен (`MCP_RATE_PER_MIN`).
- Мост `lib/mcp-bridge.tsx` (`McpBridge` в `app/(app)/layout.tsx`): данные сейфа живут в
  браузере, поэтому вкладка забирает задания long-poll'ом `/mcp/admin/bridge` и выполняет
  их через `useVault`/`useSecrets`/`searchAll`. Без вкладки агент получает `NO_BRIDGE`.
- Опасная операция `create_secret`: `pending_approval` + `approvalId` → владелец одобряет
  в Настройки → «MCP наружу» (`mcp-pending-approve`/`-reject`) → агент повторяет вызов.
  Одобрение выполняет запись сразу во вкладке; сейф должен быть разблокирован.
- Журнал безопасности: новые типы `mcp-token-issued`, `mcp-token-revoked`, `mcp-call`,
  `mcp-denied`, `mcp-approval`; аудит сервера доставляется во вкладку и пишется в IndexedDB.
- UI `components/mcp-section.tsx` + `mcp-tokens.tsx`: адрес сервера, статус моста,
  ожидающие подтверждения, выдача токена (имя, области, срок), список и отзыв,
  сниппет для Claude Desktop / Cursor (`mcp-remote`).
- `/mcp/admin/*` закрыты сессией (`proxy.ts` matcher расширен).
- Тесты: `tests/unit/mcp-access.test.ts` (8), `tests/api/test_mcp.py` (12),
  `tests/e2e/21-mcp-external.spec.ts` (полный сценарий агента). Регресс: `tsc` 0,
  `eslint` 0 ошибок, `vitest` 205/205.

### Бэклог после NF-10
- P3: NF-11 · E2EE-синхронизация — последняя задача волны 5.
- P3: MCP — SSE-поток для `tools/call` с долгим ожиданием; OAuth resource metadata
  (RFC 9728) для клиентов, которые не умеют статический Bearer.
- P3: MCP — инструмент чтения тела стикера по отдельной области `notes:read`.

---

## NF-11 · E2EE-синхронизация (2026-06-04, сделано) — волна 5 закрыта
- Ключ: 12 слов BIP39 → HKDF → AES-GCM-ключ + `spaceId` + «пароль пространства» (`lib/sync/crypto.ts`).
  Сервер видит только `spaceId` и хеш пароля; ключ хранится в `wf.sync.v1` (localStorage, локально).
- CRDT `lib/sync/crdt.ts`: LWW по полю с HLC-метками и id устройства, tombstones, векторные часы.
  Коллекции: файлы (без `processing`), стикеры (кроме `locked`), уведомления. Секреты не синкаются.
- Слепое хранилище `lib/sync-server.ts`, маршруты `/sync/devices` (POST регистрация по паролю
  пространства, GET список, DELETE отзыв) и `/sync/ops` (GET long-poll since/wait, POST push) — за сессией.
  Диск: `AI_DIR/sync/<spaceId>/space.json` + `ops.jsonl`.
- Движок `lib/sync/engine.tsx` (`SyncProvider` в `app/(app)/layout.tsx`), UI `components/sync-section.tsx`
  (раздел «Синхронизация»: create/join, фраза+QR, устройства, отзыв, выключение). Журнал: 3 новых типа.
- Тесты: `tests/unit/sync.test.ts` (5), `tests/api/test_sync.py` (5), `tests/e2e/22-sync-e2ee.spec.ts`
  (два контекста). Регресс: `tsc` 0, `eslint` 0 ошибок, `vitest` 210/210.

### Бэклог после волны 5
- P3: ротация фразы после отзыва устройства (отозванное устройство всё ещё знает старую фразу;
  сейчас UI об этом предупреждает). Ключ синхронизации под мастер-ключом при включённом замке.
- P3: синхронизация разговоров чата (`wf.chat.v1`) и записей менеджера секретов отдельной областью.
- P3: компакция `ops.jsonl` в снапшот при большом журнале.

---

## Аккаунты и админ-панель (2026-06-04, сделано)
- Учётные записи: email + пароль (scrypt), саморегистрация на /login; новый пользователь получает доступ
  только по ключу лицензии `WSX-XXXX-XXXX-XXXX-XXXX` на срок (7/30/90/365 дн.), который выдаёт админ.
  Первый админ сеется из `ADMIN_EMAIL` + `APP_PASSWORD` (вход одним паролем без email сохранён).
- Сессии серверные (`AI_DIR/users/sessions.json`), cookie `wf_session` подписана HMAC; админ может
  завершить сессии, заблокировать, сбросить пароль (смена при первом входе), удалить с данными.
- Тумблеры функций на пользователя: ai, mcp, sync, secrets, offline, telemetry + суточный лимит ИИ.
  Сервер режет в `proxy.ts` (FEATURE_DISABLED / LICENSE_REQUIRED / PASSWORD_CHANGE_REQUIRED /
  BLOCKED / ADMIN_ONLY), интерфейс скрывает экраны и разделы (`useAccount().has`).
- Изоляция: сервер — `AI_DIR/users/<uid>/` (диалоги, навыки, MCP-аудит, синк-пространства с owner),
  токены MCP хранят owner; браузер — IndexedDB `workflow-<uid>` и префикс `u:<uid>:` в localStorage
  (`lib/db/scope.ts`). Первый админ остаётся на старых именах (legacyStore) — миграция не нужна.
- Экран `/admin` (только admin): сводка, список пользователей, карточка (роль, функции, лимит, лицензия,
  пароль, сессии, блок, удаление), ключи лицензий. Файлы: `lib/users.ts`, `lib/users-server.ts`,
  `lib/request-context.ts`, `lib/account.tsx`, `app/ai-api/auth/*`, `app/admin/api/*`,
  `components/screen-admin.tsx`, `admin-user-card.tsx`, `admin-licenses.tsx`.
- Тесты: `tests/api/test_admin.py` (10), `tests/unit/users.test.ts` (9), `tests/e2e/23-admin-accounts.spec.ts`;
  `tests/e2e/global-setup.ts` даёт cookie админа старым сценариям. Тест-агент — 100% (iteration_27).

### Бэклог
- P2: email-подтверждение и восстановление пароля по письму (нужен SMTP-провайдер).
- P2: аудит действий админа в журнале безопасности (сейчас — только серверный лог).
- P3: миграция локальной базы первого админа на схему `workflow-<uid>`.

### Полировка дизайна админки и входа (2026-06-04)
- /login: переключатель «Вход / Регистрация» получил собственные стили в `globals.css` (`.login-mode`) — раньше на /login не грузились стили настроек и он выглядел как ссылки.
- /admin: цветные акценты сводки (ok/warn/danger), аватары с инициалами и цветом роли, пилюли роли и статуса с точкой, колонки «лицензия / ИИ сегодня / вход» с подписями, форма создания — во всю ширину под заголовком, карточка пользователя с шапкой-аватаром и разделителями секций, пустое состояние карточки.
- Стены доступа (`AccessWall`) — с логотипом, как на входе. Регресс: e2e 23 PASS, css-split PASS.

### Багфикс: застрявший сплэш «WORKSPACEX» на /login (2026-06)
- Симптом: у пользователя /login показывал только центрированное «WORKSPACEX», форма входа не появлялась.
- Причина: bootstrap-скрипт в `app/layout.tsx` вешал `html.lock-pending` (CSS скрывает весь body) при `wf.lock.config.enabled=true` в localStorage. Класс снимается только в lib/store/lock.tsx внутри `(app)`-лейаута; страница `/login` эти провайдеры не монтирует → заглушка висела вечно у всех, кто включал мастер-ключ.
- Фикс: bootstrap не добавляет `lock-pending` при `location.pathname==='/login'`; `app/login/page.tsx` снимает класс на mount (подстраховка). Проверено скриншотом с эмуляцией localStorage-замка. Требует `npx next build && restart frontend` (прод-сборка).
