# WorkSpaceX — доводка до продакшена (2026-09-09)

Репозиторий: `C:\Users\admin-pc\Desktop\HERMES\work-pc-main`. Статусы: `.hermes/plans/status-prod-*.md`.
Решения пользователя: одна мультимодальная модель **qwen2.5vl:3b** (видит фото + читает документы; манифест в реестре Ollama проверен, HTTP 200); при выборе папки хранения существующие файлы копируются туда автоматически; удаление — с галочкой «Удалить базу, модель и все данные» (по умолчанию ВКЛ).

## P1. Иконка приложения
- Иконка в EXE: уже `signAndEditExecutable: true`. НЕ хватает: иконки окна (BrowserWindow) и иконки в UI мастера.
- `main.mjs`: `BrowserWindow` для setup и workspace получают `icon: path.join(here, '../assets/icon.png')`.
- Проверка: извлечение иконки из собранного EXE, наличие `icon` в обеих BrowserWindow.

## P2. Папка хранения как главное хранилище
- **Корень бага**: `cloud-section.tsx` выбирает папку через `window.prompt` — в Electron он НЕ работает (всегда null). 
- Новый мост `workspacex:pick-folder` → `dialog.showOpenDialog(workspace, { properties:['openDirectory','createDirectory'] })` → путь.
- `cloud-section.tsx`: кнопка выбора папки зовёт мост; фолбэк в браузере — ручной ввод пути (текстовое поле), НЕ prompt.
- При выборе папки: **существующие объекты автоматически копируются** в неё (серверно: PUT storage-root с параметром `migrate:true`; после записи новых байтов метаданные объекта обновляются на relPath; readStorageRoot продолжает отдавать старые объекты через fallback на objects-хранилище).
- Все новые загрузки — в папку (уже реализовано в uploadFileImpl, проверить anche «файлы сейфа»/вложения).

## P3. Фотографии анализируются (единая модель)
- `ollama-release.json` + `config.mjs runtimeEnv` + `downloads.mjs ensureModel`: модель **`qwen2.5vl:3b`** вместо qwen2.5:3b (modelBytes ~3.4e9, имя в UI «Qwen2.5-VL 3B · зрение+текст»).
- `lib/file-analyzer.ts`: фото → `/api/chat` c `images:[base64]` моделью из `OLLAMA_MODEL` (больше НЕ нужен отдельный moondream/pull); тот же промпт-конвейер для всех типов.
- `lib/llm/ollama.ts` и всё, что хардкодило `qwen2.5:3b` — на env.OLLAMA_MODEL.
- Чат и настройки показывают новую модель (проверить `NEXT_PUBLIC_AI_MODEL_LABEL` в runtime.env).

## P4. Промпт анализа (правильно и красиво)
- `lib/file-analyzer.ts`: системный промпт улучшить: роль «архивариус», строгий JSON {title ≤8 слов, description ≤48 слов, tags?: до 5}, русский язык, без воды, описание = что это + что внутри + чем полезно; для фото: объекты, сцена, текст на фото (OCR-суть), настроение/цвет; для ZIP: структура и главное содержимое; форматировать description в 1-2 предложения.
- Добавить юнит-тест промпт-сборки (fixture: zip listing, exif) — без реальной модели.

## P5. mail.tm «не выдал доступ к ящику»
- `lib/temp-mail.ts`: найти точную причину (похоже, адрес создаётся, но token/password не сохраняются или login делается не тем паролем — читать код createMailtm/openMailtm).
- Ошибка должна быть человекочитаемой и с кодом; добавить retry логина при 401; при необходимости — 1Fichier-подобных прокси НЕ добавлять, работать напрямую.
- Тест: unit на парсинг ответов mail.tm (fixture JSON), на сохранение credentials.

## P6. «Открыть на ПК»/«Показать в папке»
- **Корень**: у файлов библиотеки/сейфа нет absPath на клиенте и путь не передаётся в мост; мост требует путь внутри storage-root — без выбранной папки всё честно падает.
- После P2 (папка выбрана) сервер отдаёт `absPath` в file view (только админ), клиент передаёт в `openPath/revealInExplorer`; при отсутствии папки кнопки дизейбл с tooltip «Выберите папку хранения в настройках диска».
- Проверить `handleWorkspace` trusted-проверку (workspaceUrl!) — reveal из окна настройки не работает по дизайну, только из workspace.

## P7. Виджет «Хранилище»
- Убрать проценты и квоту 128 МБ; показывать **«N файлов»** (и суммарный объём занятого, если легко) вместо «0% · 12 КБ из 128 МБ».
- `app-shell.tsx` (sidebar-storage) + источник stats (`lib/store/data.tsx` или где считается usedPct): заменить на count файлов + bytes.

## P8. «Сейф секретов закрыт» после смены мастер-ключа
- `secrets-store.tsx` (строка ~232): `ready` становится true только после `ensureSecretsSession()`; при смене мастера adoptMasterSession вызывается ДО completeUnlock, а сессия секретов привязана к `v.lockEpoch` — после changeMaster эпоха меняется, ready=false, но attempt идёт 10×120 мс и может проиграть гонку с перезаписью сеанса.
- Чинить: после успешного changeMaster/recovery явно перезапускать секретную сессию (перевыпустить ensureSecretsSession по событию смены мастера, увеличить retry-окно до ~3 с, писать в journal если не удалось), чтобы «закрыт» не висел при разблокированном сейфе.
- Тест: state-машина смены мастера → ready=true без ручного relock.

## P9. Мастер настройки (первый экран) — красиво и удобно
- `desktop/ui/setup.html/css/js`: редизайн по access.css-паттернам проекта (тёмный графит, фирменный зелёный акцент, логотип из assets/icon.png): один экран-визард: выбор «Главный ПК / Подключиться» → форма с прогрессом скачивания модели qwen2.5vl:3b (проценты из setup:progress уже идут), статус по шагам.
- Добавить показ **имени модели и размера** перед скачиванием.
- Экран входа в приложение (`app/login/page.tsx`) — привести к той же системе (шрифты/отступы/акценты по globals.css, логотип).

## P10. Удаление с данными
- `desktop/assets/installer.nsi`: страница uninstall с чекбоксом «Удалить также базу, модель и все данные» (по умолчанию ВКЛ): при ВКЛ — `RMDir /r` для `%APPDATA%\WorkSpaceX` (база, модель в models/, runtime/, logs/, secrets.bin — всё).
- Текст на русском, подтверждение.
