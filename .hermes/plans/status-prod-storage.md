# Статус: prod-storage (P2, P3, P4, P6, P7 из 2026-09-09-production.md)

- [done] P2 · lib/cloud-store.ts: `setStorageRootMigrating(raw, migrate)` → `{root, migrated?}`; при migrate:true после записи storage-root.json все живые объекты переносятся в новую папку (`migrateObjectsIntoRoot`): байты из прежней папки (relPath) или объектного хранилища (path → getObject), запись через `writeIntoRoot` (коллизии «имя-1.ext»), метаданные обновляются (relPath, path=relPath) только у успешных; ошибка одного файла не фатальна — отчёт `{copied, failed, errors[]}`; повторный выбор той же папки и выбор без флага ничего не дублируют
- [done] P2 · app/ai-api/cloud/storage-root/route.ts: PUT принимает `{root, migrate?:boolean}`, отвечает `{root, migrated?}`
- [done] P3 · lib/file-analyzer.ts: убраны moondream/VISION_MODEL/VISION_PROMPT/DESCRIBE_MODEL/ensureVisionModel (/api/pull) полностью; одна мультимодальная модель `analyzerModel()` = env.OLLAMA_MODEL (фолбэк qwen2.5vl:3b); фото анализируется тем же /api/chat через `images:[base64]` в общем промпт-конвейере; lib/llm/ollama.ts: убран хардкод `model.startsWith('qwen2.5:3b')` (num_predict 768)
- [done] P4 · lib/file-analyzer.ts: системный промпт «архивариус» (DESCRIBE_SYSTEM): строгий JSON {title ≤8 слов, description ≤48 слов в 1–2 предложениях, tags до 5}, русский, без воды; фото — что изображено + текст на фото; ZIP — структура и содержимое; документ — тип, о чём, ключевые факты; сборка промпта вынесена в чистую `buildAnalysisMessages()`; tags парсятся (cleanTags: дедуп, лимит 5) и сохраняются в метаданные (analysisTags: CloudFile/AnalysisPatch/setFileAnalysis/getAnalysisInput, GET /ai-api/analyze отдаёт `tags`)
- [done] P6 · lib/cloud-store.ts: `driveView()` отдаёт админу `absPath` (root+relPath, проверка «внутри корня») только при выбранной папке и наличии relPath (тип CloudFileView); plumbing до клиента: CloudLibraryItem.absPath (lib/cloud-types.ts) → VaultFile.absPath (lib/data.ts) → loadCloud (lib/store/data.tsx) → screen-library (absPathOf уже был) → LibraryViewer
- [done] P6 · components/library-viewer.tsx: кнопки «Открыть на ПК»/«Показать в папке»: без моста — «Доступно в приложении для Windows»; мост есть, папка не выбрана/absPath нет — дизейбл с tooltip «Выберите папку хранения в настройках диска»
- [done] P2/P6 · components/cloud-section.tsx: мост `window.workspacexDesktop.pickFolder(current?) → Promise<string|null>` в контракте, вызов через optional chaining; фолбэк в браузере — встроенное текстовое поле пути (НЕ window.prompt); при выборе папки уходит `migrate:true`, отчёт о переносе показывается во flash («Перенесено файлов: N», при failed — причина первого); components/cloud-section.css: стили .cloud-storage-manual
- [done] P7 · lib/store/data.tsx: VaultStats без quota/usedPct (остались files/bytes/processing); components/app-shell.tsx: виджет «Хранилище» = «N файлов» + суммарный объём (fmtBytes, data-testid storage-size), прогресс-бар и проценты убраны, при обработке — «обрабатывается: N»; app/globals.css: .storage-bar удалён, .storage-meta.busy добавлен; components/screen-settings.tsx: подписи без процентов/квоты (объём + файлы)
- [done] lib/data.ts: fmtBytes получил уровень ГБ (КБ/МБ/ГБ человекочитаемо); VaultFile.absPath
- [done] Тесты: tests/unit/cloud-storage.test.ts +3 (миграция той же папки + absPath; переезд в новую папку: коллизии -1, битый источник не фатален; migrate opt-in), tests/unit/file-analyzer.test.ts +10 (промпт архивариуса, buildAnalysisMessages system/user/images/пустая выборка, analyzerModel из OLLAMA_MODEL, parse tags)

## Верификация
- `node_modules/.bin/tsc --noEmit` — чисто (exit 0, финальный прогон после всех правок).
- `vitest run tests/unit` — 336/336 зелёные, 41 файл (baseline до правок тоже прогнан: 0 падений).
- `vitest run tests/unit/cloud-storage.test.ts tests/unit/file-analyzer.test.ts` — 53/53 (в т.ч. 13 новых).
- Остаточные вхождения 'qwen2.5:3b'/'moondream' в lib/ и app/: нет (grep). Единственное осознанное исключение — каталог настроек OLLAMA_TAGS в lib/llm/models.ts, см. примечания.
- tests/unit/css-split.test.ts стережёт монолит _monolith.css: удаление .storage-bar из globals.css валит guard, поэтому правило оставлено (не рендерится) с комментарием.

## Примечания для координатора
- `desktop/runtime.env` НЕ тронут (зона координатора): `OLLAMA_MODEL=qwen2.5:3b` там останется, пока его не сменят на qwen2.5vl:3b; код уже берёт модель из env (анализ) и подпись из NEXT_PUBLIC_AI_MODEL_LABEL (lib/data.ts CLOUD_MODEL_LABEL).
- lib/llm/models.ts (OLLAMA_TAGS, каталог настроек чата) сознательно не менял: это карта «профиль→тег» на 4 модели для пикера настроек, модуль явно «без env» (читают сервер и браузер), а подмена любого выбора на env.OLLAMA_MODEL была бы «тихой подменой», которую lib/llm запрещает намеренно. Смена каталога на одну модель qwen2.5vl — за координатором (реестр Ollama + настройки). Анализ и хардкоды конвейера переведены на env полностью.
- Мост: ожидается `pickFolder(current?: string | null) => Promise<string | null>` в preload-workspace.cjs (desktop/**, другой исполнитель); вызов уже готов, в браузере работает фолбэк-поле.
- Контракт PUT /ai-api/cloud/storage-root: `{root: string, migrate?: true}` → `{root: string|null, migrated?: {copied, failed, errors: string[]}}`; GET /ai-api/analyze теперь может отдавать `tags: string[]`.
- screen-settings.tsx: две строки тронуты по необходимости (убраны stats.usedPct/stats.quota из подписей) — иначе tsc падал после удаления полей из VaultStats.
- lib/cloud-types.ts, lib/data.ts: минимальные добавления (absPath в CloudLibraryItem/VaultFile, ГБ в fmtBytes) — нужны для цепочки absPath до просмотрщика и «объём в ГБ» без дублирования форматтеров.
