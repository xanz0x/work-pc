# WORKFLO<b>W</b> · local ai vault

> Локальное ИИ-хранилище с приватностью как ядром продукта: библиотека файлов, живая карта памяти, чат с локальной моделью и полноценная система блокировки — всё на своём устройстве, без облака.

[![Stack](https://img.shields.io/badge/Next.js-16-black)](https://nextjs.org) [![React](https://img.shields.io/badge/React-19-blue)](https://react.dev) [![TypeScript](https://img.shields.io/badge/TS-strict-3178c6)](https://typescriptlang.org) [![Tailwind](https://img.shields.io/badge/Tailwind-v4-38bdf8)](https://tailwindcss.com)

---

## Оглавление

1. [Философия](#философия)
2. [Быстрый старт](#быстрый-старт)
3. [Четыре экрана](#четыре-экрана)
4. [Система блокировки](#система-блокировки-安全)
5. [Архитектура](#архитектура)
6. [Дизайн-система «Графит»](#дизайн-система-графит)
7. [Адаптивность](#адаптивность)
8. [Структура проекта](#структура-проекта)
8.1 [Документация](#документация)
9. [Данные и хранение](#данные-и-хранение)
10. [Безопасность: честные границы](#безопасность-честные-границы)
11. [Разработка](#разработка)
12. [Дорожная карта](#дорожная-карта)

---

## Философия

**WorkSpaceX** — это не «ещё один Notion». Это локальный сейф с ИИ, где приватность — не галочка в настройках, а фундамент:

- **Локально** — файлы, индекс и ключи не покидают устройство. Чат — исключение: пока локального движка нет, ответы даёт внешняя модель, и продукт спрашивает согласие и пишет каждый такой ход в ленту событий.
- **Красиво** — строгая дизайн-система «Графит»: глубина слоями тона, ни одного blur/тени.
- **Живо** — интерфейс отвечает на каждое действие: числа докручиваются, узлы дышат, плитки летают.
- **Безопасно** — настоящая криптография WebCrypto (PBKDF2 310k + AES-GCM), а не декоративные бейджи.

Продукт позиционируется в нише между Obsidian (локальность, платные сервисы) и AnythingLLM (локальный RAG-чат), добавляя то, чего нет ни у кого: **живую карту памяти + MCP-управляемость**.

---

## Быстрый старт

```bash
# Требования: Node.js ≥ 20, pnpm ≥ 9
npm i -g pnpm                 # если pnpm ещё нет

pnpm install                  # установка зависимостей
pnpm dev                      # dev-сервер → http://localhost:3000
```

> В этом рабочем каталоге сервер запускается на `127.0.0.1:3457` (см. «Разработка»).

Сборка продакшена:

```bash
pnpm build
pnpm start
```

Первый запуск показывает **библиотеку с демо-данными** (13 файлов, стикеры, разговор) — это позволяет оценить все сценарии без своих файлов. Демо названо своим именем: баннер над библиотекой, плашка «демо» на каждой карточке и метка «ДЕМО» в шапке. Кнопка **«Начать с чистого сейфа»** в баннере убирает ровно демо-объекты и не трогает то, что вы добавили или импортировали; демо-корпус живёт отдельным модулем (`lib/demo-seed.ts`), грузится динамически и в основной бандл не попадает.

---

## Четыре экрана

### 📚 Библиотека

Два слоя памяти: **файлы, которые пришли** — и **стикеры, которые вы подумали**.

- Загрузка файлов → конвейер: OCR → автотеги → ИИ-описание → индексация.
- **Доска плиток** (drag & drop): мозаика с размерами `sm/wide/tall/xl`, закрепление наверх, перетаскивание файлов в кластер-чип, полная клавиатурная альтернатива (Space → стрелки → Space, Escape-отмена), aria-live-анонсы.
- Кластеры-фильтры, семантический поиск, инспектор объекта справа (превью, метаданные, ИИ-описание, прикреплённые стикеры, связанные объекты).
- Стикеры: временные (с таймером распада) и постоянные, могут быть заперты на пароль.

### 🗺 Карта памяти

Живая нейрографическая карта: узлы-файлы, связи по смыслу, ядро **WORKSPACEX CORE** в центре.

- Зум к области под курсором, отдаление возвращает ядро в центр.
- Клик по узлу → инспектор; поиск подсвечивает найденное акцентной кромкой с бегущим лучом.
- Метрики (связи, кластеры) анимируются; легенда кластеров внизу.

### 💬 Чат с ИИ

Разговор поверх собственного хранилища.

- Retrieval-trace: каждый ответ показывает, **из каких источников** он собран.
- Композер с рельс-подсказками, источник под курсором приподнимается, знак модели дышит при генерации.
- Локальный движок (Qwen 2.5 7B) или облачный — переключается в Настройках.
- Объекты под файловым ключом исключаются из контекста: ИИ физически не может процитировать защищённое.

### ⚙️ Настройки

Конвейер обработки (OCR, автотеги, слежение за папками), движок и модель, приватность (маскирование, телеметрия), уведомления, хранилище, опасная зона — и **секция «Безопасность»** (см. ниже).

---

## Система блокировки 安全

Полноценная двухуровневая защита, реализованная по плану [`docs/architecture/lock-system.md`](./docs/architecture/lock-system.md).

### Уровень A — мастер-ключ

| Возможность | Как |
|---|---|
| PIN **ровно 6 цифр** или пароль ≥ 8 символов | Настройки → Безопасность → «Настроить мастер-ключ» |
| Экран блокировки | Полноэкранная сцена «хранилище»: сетка-пол в перспективе, метеоры, PIN-ячейки с кареткой |
| Кнопка-замок | У шестерёнки в топбаре + хоткей **Ctrl+Shift+L** |
| Автоблокировка простоя | Никогда / 5 / 10 / 15 / 30 минут (тик 5 с, активность = click/keydown/pointermove) |
| Анти-брутфорс | Счётчик неудач → задержка 1s→2s→4s…, busy-флаг на время деривации PBKDF2 |
| Смена мастера | Полный re-setup с подтверждением старого; сессия остаётся разблокированной |
| Синхронизация вкладок | BroadcastChannel `workflow-lock`: закрыл в одной — закрылось везде |
| SSR-безопасность | Inline-bootstrap в `<head>` ставит класс `lock-pending` до гидратации — первый кадр не мигает |

### Уровень B — файловые ключи

- Любой файл или стикер можно **«Поставить на ключ»** — повторный ввод при каждом открытии, поверх мастер-разблокировки.
- Хранение: `wrappedFileKey = AES-GCM(masterKey, fileKey)` — при **смене мастера файловые ключи пере-упаковываются автоматически** (пере-wrap без знания старых паролей).
- **Redact-слой**: защищённые объекты вырезаются из поиска (`search.ts redactIds`), retrieval-trace чата, подсказок и инспекторов — вместо содержимого честная строка «Под ключом».
- Инвариант целостности (аудит при старте): `locked ⇒ шифртекст существует`, иначе блокировка честно снимается с событием в ленту.
- **Забыл мастер** → сброс стирает замок и все файловые ключи (файлы остаются) — предупреждение при создании.

### Криптография

```
PBKDF2-HMAC-SHA256, 310 000 итераций, salt 16B  →  AES-GCM-256
```

Нативный WebCrypto, ноль зависимостей. Деривация ≈ 104 мс на среднем железе. Секреты хранятся как `ct:iv` (base64) — в localStorage нет ни одного plaintext-пароля.

---

## Менеджер секретов (модуль VAULT v1)

Пятый экран (`ScreenId: 'vault'`) и **один** пункт сайдбара в секции «Секреты». Вся глубина — внутри экрана: три колонки «навигация → список → деталь».

### Что умеет (CORE v1)

| Возможность | Как сделано |
|---|---|
| 12 типов записей | login, api, seed, card, ssh, note, recovery, wifi, license, identity, passkey-meta, custom (`lib/secrets.ts` → `TYPE_META`) |
| Конструктор полей | 9 видов полей, у любого можно включить `secret` → значение уходит в шифртекст |
| Папки, теги, избранное, корзина | папки с цветом и счётчиком, мягкое удаление → восстановление → безвозвратное удаление |
| Генератор | пароль/PIN/hex/UUID на `crypto.getRandomValues`, произносимый режим, локальный strength-meter (энтропия минус штрафы) |
| TOTP (RFC 6238) | Base32 + HMAC-SHA1/256/512 на WebCrypto, 6/8 цифр, period, импорт `otpauth://`, обратный отсчёт. **Сверено с официальными векторами RFC 6238** (`scripts/totp-check.ts`) |
| Буфер обмена | автоочистка по типам (пароль 10с / TOTP·CVV 5с / логин 30с / настраивается 5·10·30·60·никогда), тост с отсчётом и «Очистить сейчас» |
| Показ секрета | маска по умолчанию, авто-скрытие 8с, гашение при уходе со страницы, blur окна и блокировке; «копировать без показа» |
| Поиск | новый `HitKind = 'secret'` в `searchAll()` + Ctrl+K; в индекс уходят только название, тип и теги — содержимое недоступно поиску **по построению** |
| Импорт | CSV (KeePassXC / Bitwarden / 1Password / LastPass) и Bitwarden JSON, превью до записи, лимит 5 МБ и 5000 записей, whitelist полей |
| Экспорт | зашифрованный снимок (AES-GCM поверх PBKDF2 600k, свой пароль) и plaintext CSV/JSON — с красным предупреждением и вводом слова подтверждения |
| Бэкапы | снимок под мастер-ключом в браузере, ротация 5 копий, авто не чаще раза в час, restore с превью |
| Panic Lock+ | Ctrl+Shift+L: замок + очистка буфера + гашение раскрытий + обнуление ключей сеанса (дубль хоткея в app-shell убран) |
| Иконки сайтов | тумблер, **по умолчанию ВЫКЛЮЧЕН**; наружу уходит только домен, картинка живёт в сейфе как b64 (никаких внешних `src`) |
| Настройки | секция «Менеджер секретов» в Настройках; тумблер «исключать секреты из ИИ» включён и заблокирован от выключения |

### Криптография модуля (надстройка, не замена)

```
мастер-ключ сеанса (PBKDF2-HMAC-SHA256 600 000 → AES-GCM-256)
  └─ wrap → SEK: случайные 32B, wf.secrets.sek.v1
       └─ HKDF-SHA256(salt = SHA-256('wf.secrets.' + entryId))
            └─ AES-GCM-256 → значение поля как `ct:iv`
```

SEK нужен потому, что мастер-`CryptoKey` неэкстрагируем: вывести из него ключи на запись нельзя. Сырьё SEK живёт только в памяти вкладки и обнуляется вместе с сеансом.

**KDF поднят 310 000 → 600 000** (OWASP 2024). Миграция ленивая: при первом успешном unlock старая схема расшифровывается прежним ключом, всё завёрнутое (обёртки файловых ключей, секреты стикеров, SEK) переупаковывается под новый ключ, состояние замка перезаписывается, а старое кладётся в `wf.lock.migrate.backup.<ts>`. Тот же механизм (`rewrapAll`) теперь честно работает и при смене мастер-ключа — раньше файловые ключи после смены мастера оставались завёрнутыми старым ключом.

### Честные ограничения модуля

- Очистку буфера обмена делает браузер: историю буфера сторонних программ ОС отменить нельзя (сказано в UI).
- Вложения к записям, Password Health, BIP39-seed, Wi-Fi QR — вне CORE v1 (см. дорожную карту).
- Восстановление из бэкапа работает как импорт: записи добавляются, существующие не затираются.

---

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│ app/layout.tsx                                          │
│  ├─ bootstrap-скрипт замка (синхронно, до гидратации)    │
│  ├─ RedactedProvider  ←-- реестр защищённых объектов     │
│  └─ VaultProvider      ←-- единое состояние приложения   │
└─────────────────────────────────────────────────────────┘
          │                                  │
   ┌──────▼──────┐                    ┌──────▼──────┐
   │ app-shell   │  lock-состояние,   │  4 экрана   │
   │ (каркас)    │  Ctrl+Shift+L,     │ library/map │
   │             │  storage-синк      │ chat/settings│
   └─────────────┘                    └─────────────┘

lib/crypto-vault.ts   PBKDF2+AES-GCM ядро (zero-deps)
lib/lock-store.ts     синхронный bootstrap, конфиг, аудит инвариантов
lib/redact-context    что скрыто в этой вкладке (locked \ unlocked)
hooks/use-file-keys   сессионный мастер, wrapped-ключи, миграция 10.6
hooks/use-board-dnd   жесты доски: Pointer Events, автоскролл, клавиатура
hooks/use-dialog      ловушка фокуса, Escape по стеку диалогов, возврат фокуса
lib/board-layout.ts   чистая модель мозаики (тестируется в Node)
```

**Принципы:**

- **Единый сейф** — все экраны читают одно состояние (`useVault`); числа нигде не расходятся.
- **Чистые слои доски** — модель (`board-layout`) без React/DOM, жесты (`use-board-dnd`) без состояния, вид (`library-board`) — контролируемый.
- **Синхронный bootstrap замка** — статус читается до первого рендера; заглушка `lock-pending` не даёт «мигнуть открытым».
- **Unlock-факт живёт в памяти вкладки** (не в localStorage) — новая вкладка = новый ввод пина.
- **Кастомный Next.js 16** — см. `AGENTS.md`: API могут отличаться от известных, перед незнакомым кодом читать `node_modules/next/dist/docs/`.

---

## Дизайн-система «Графит»

Файл-источник истины: [`app/globals.css`](./app/globals.css) (слои идут хронологически, каждый добавляет, не ломает):

| Слой | Что добавил |
|---|---|
| **v3** | Базовая система: тона `--bg/--panel`, волосяные границы `--border*`, акцент `#2fbe7e`, IBM Plex, 4 экрана |
| **v3.1** | Премиум-полиш: `NumTicker` (докручивающиеся числа), `Beam` (луч по границе), живые часы, дыхание логотипа, штриховка хранилища |
| **LOCK v3.2** | Экран блокировки, кнопка-замок, секция «Безопасность», bootstrap-заглушка |
| **FILE-KEYS v3.2b** | Бейджи «под ключом», модалки файловых ключей |
| **REDACT v3.2c** | Красакт-строки в поиске/чате, пунктирные границы скрытого |
| **POLISH v3.3** | `make-interfaces-feel-better`: концентрические радиусы, ghost-кнопки, акцентная заливка CTA, табличные числа |
| **INPUT-COUNT v3.4** | Живые счётчики длины, PIN-линейки, каретка ячейки |
| **LOCKSCREEN v3.5** | Сцена «хранилище»: retro-grid пол в перспективе, усиленные метеоры, 2-зонная карточка, гравировка |
| **v3.6–v3.7** | Фиксы читаемости монограммы, CTA, производительность (GPU-композит вместо repaint) |
| **INPUT v4.0 / CLUSTERS v4.1 / SIDEBAR v4.2** | Широкие настройки, просторные поля, свёрнутые кластеры, иконки секций без капса |
| **RESPONSIVE v3.8** | Полная адаптивность (см. ниже) |

**Законы системы:**

1. Глубина — **тонами и волосяными границами**. Запрещены blur, box-shadow, полупрозрачные пятна.
2. Один акцент `#2fbe7e` — только для активных состояний и интерактива.
3. Радиусы 3–4px (концентричность: внешний = внутренний + padding).
4. Числа — всегда `tabular-nums` (`.num`).
5. `transition-property` указывается точечно — никогда `all`.
6. Анимации уважают `prefers-reduced-motion` (глобальный блок глушит всё).
7. Справочные паттерны Magic UI берутся curl'ом (`https://magicui.design/r/<имя>.json`) и портируются вручную без зависимости `motion`.

---

## Адаптивность

Единая сетка брейкпоинтов (детали — блок `RESPONSIVE v3.8` в конце globals.css):

| Ширина | Поведение |
|---|---|
| ≥ 1280 | Полный расклад: сайдбар 248px + контент + инспекторы |
| ≤ 1280 | Инспектор библиотеки 320 → 280px |
| ≤ 1180 | Стол источников чата → фиксированное наложение |
| ≤ 1100 | Карта: компактный HUD; **чат: рельс → выдвижной ящик**; инспектор библиотеки под контентом |
| ≤ 1024 | Карточки в 2 колонки |
| ≤ 768 | Сайдбар → иконочная колонка 56px; чипы-фильтры — горизонтальный скролл; легенда карты — строкой |
| ≤ 520 | Экран блокировки: компактная типографика |
| ≤ 480 | Сайдбар → **нижняя таб-панель**; модалки `100vw − 24px` |
| ≤ 380 | Замок: статус-строка SESSION ⟷ ПОПЫТКИ; PIN-ячейки flex |
| `hover: none` | Тач: тап-зоны ≥ 44px, hover-состояния → always-visible |

Производительность анимаций: сетка-пол и метеоры работают на GPU (transform-only, фиксированный расклад без `Math.random` в рендере); на слабых машинах (`update: slow`) декоративные анимации отключаются.

---

## Структура проекта

```
vault-core-arch/
├── app/
│   ├── layout.tsx          # корневой layout: bootstrap замка, провайдеры
│   ├── page.tsx            # выбор активного экрана по состоянию сейфа
│   └── globals.css         # дизайн-система «Графит» (все слои v3→v4.2)
├── components/
│   ├── app-shell.tsx       # каркас: сайдбар, топбар, статус-бар, замок
│   ├── screen-library.tsx  # библиотека + инспектор + стикеры (1638 строк)
│   ├── library-board.tsx   # контролируемая доска плиток
│   ├── screen-map.tsx      # карта памяти на canvas (1586 строк)
│   ├── screen-chat.tsx      # чат с ИИ
│   ├── screen-vault.tsx     # менеджер секретов: 3 колонки, тост буфера
│   ├── vault/               # nav, list, detail, editor, generator, io, totp-cell, secret-value
│   ├── secrets-section.tsx  # секция настроек модуля секретов
│   ├── screen-lock.tsx     # полноэкранная блокировка (PIN/пароль)
│   ├── screen-settings.tsx # настройки конвейера/приватности
│   ├── security-section.tsx# секция «Безопасность»
│   ├── command-palette.tsx # палитра Ctrl+K
│   ├── notifications.tsx   # лента событий сейфа: фильтры, сводка-контейнер
│   ├── dialog-shell.tsx    # модалка с ловушкой фокуса для разметки без хуков
│   ├── dropdown.tsx        # сегменты/выпадашки
│   ├── icons.tsx           # 60+ SVG-иконок (currentColor)
│   ├── chat/               # composer, messages, retrieval-trace, source-desk…
│   └── ui/                 # beam, num-ticker (порты Magic UI)
├── hooks/
│   ├── use-board-dnd.ts    # жесты доски: pointer, автоскролл, клавиатура
│   ├── use-file-keys.ts    # файловые ключи, миграция, сессионный мастер
│   ├── use-ai-chat.ts      # цикл агента: SSE-поток, скиллы, подтверждения
│   ├── use-dialog.ts       # диалоги: ловушка фокуса, Escape, стрелки по спискам
│   └── use-persisted-state.ts # localStorage-состояние без гидратационных ям
├── lib/
│   ├── vault-store.tsx     # ЕДИНЫЙ СЕЙФ: всё состояние + действия
│   ├── crypto-vault.ts     # PBKDF2+AES-GCM, zero-dependency
│   ├── lock-store.ts       # конфиг замка, синхронный bootstrap, аудит
│   ├── redact-context.tsx  # реестр скрытых объектов
│   ├── board-layout.ts     # чистая модель мозаики
│   ├── search.ts           # семантический поиск с redact-поддержкой
│   ├── secrets.ts          # модель менеджера секретов: типы, шаблоны, фильтры
│   ├── secrets-crypto.ts   # SEK + HKDF на запись, портируемые снимки
│   ├── secrets-store.tsx   # контекст модуля: CRUD, буфер, бэкапы, импорт
│   ├── secrets-totp.ts     # RFC 6238 на WebCrypto
│   ├── secrets-gen.ts      # CSPRNG-генераторы и локальный strength-meter
│   ├── secrets-io.ts       # CSV/JSON импорт с валидацией, экспорт
│   ├── lock-migrate.ts     # KDF 310k→600k и пере-упаковка обёрток
│   ├── data.ts             # типы файла, кластеры, движки, форматирование
│   ├── demo-seed.ts        # демо-корпус вне основного бандла (файлы, стикеры, разговор)
│   ├── exclusive.ts        # идемпотентность мутаций: один клик — один результат
│   ├── notes.ts            # стикеры: TTL, замки
│   ├── graph.ts            # построение связей карты
│   └── chat-data.ts        # шаблоны ответов офлайн-режима
├── ai/                     # файловый слой ИИ: system.md, skills/, mcp/, sessions/
├── docs/                   # ВСЯ документация (см. docs/README.md)
│   ├── audit/              # экспертный аудит: интерактивный PDF + как пересобрать
│   ├── architecture/       # система блокировки, менеджер секретов
│   └── product/            # исходные задания и планы модулей
├── scripts/                # totp-check.ts, генератор PDF-аудита
├── tests/api/              # интеграционные тесты /ai-api/*
├── test_reports/           # отчёты приёмки (свежие + archive/), см. README внутри
├── public/                 # иконки приложения (svg/png) + PDF аудита
├── AGENTS.md / CLAUDE.md   # правила для ИИ-агентов (Next 16!)
└── package.json
```

---

## Документация

Вся проза — в каталоге [`docs/`](./docs/README.md); в корне остаётся только этот
файл и правила для ИИ-агентов.

| Документ | О чём |
|---|---|
| [`docs/audit/`](./docs/audit) | Экспертный аудит и план перехода «прототип → приложение» (интерактивный PDF, 31 стр.) |
| [`docs/architecture/lock-system.md`](./docs/architecture/lock-system.md) | Система блокировки: мастер-ключ, файловые ключи, инварианты |
| [`docs/architecture/secrets-vault.md`](./docs/architecture/secrets-vault.md) | Менеджер секретов: устройство модуля и честные ограничения |
| [`docs/product/`](./docs/product) | Исходные задания и планы модулей |
| [`memory/PRD.md`](./memory/PRD.md) | Живой продуктовый документ: что сделано и что дальше |

---

## Данные и хранение

Всё состояние — `localStorage` с префиксами:

| Ключ | Содержимое |
|---|---|
| `wf.files.v1` | корпус файлов |
| `wf.notes.v1` | стикеры (секреты — `ct:iv` при активном замке) |
| `wf.chat.v1` | разговоры |
| `wf.notifs.v1` | лента событий |
| `wf.settings.v1` | конфигурация конвейера |
| `wf.lock.config` | замок: включён, метод, автоблокировка |
| `wf.lock.state` | salt + verifier + счётчик неудач |
| `wf.vault.keys.<id>` | wrapped-ключи файлов (`ct:iv`) |
| `wf.vault.keys.migrated` | маркер одноразовой миграции стикеров |
| `wf.filekeys.lockedlist` | список id под ключом (для redact между вкладками) |
| `wf.secrets.v1` | записи менеджера секретов (`version: 1`, секретные поля — `ct:iv`) |
| `wf.secrets.folders.v1` / `.settings.v1` / `.backups.v1` | папки / настройки модуля / зашифрованные бэкапы |
| `wf.secrets.sek.v1` | ключ сейфа секретов, завёрнутый мастер-ключом |
| `wf.lock.migrate.backup.<ts>` | снимок схемы замка перед миграцией KDF 310k→600k |

**Не хранится никогда:** мастер-ключ, пароли файлов, факт разблокировки (только память вкладки).

Стирание: Настройки → «Опасная зона» → полный wipe сейфа. Отдельно от него — «Начать с чистого сейфа» в баннере библиотеки: убирает только демо-объекты (`demo: true`), пользовательские данные остаются.

---

## Безопасность: честные границы

Система защищает **от чужих глаз и рук** — коллеги за твоим ноутбуком, 家庭 гости, случайный показ экрана. Она **не защищает** от:

- форензики диска (localStorage читается DevTools владельца машины);
- вредоносных расширений браузера;
- физического изъятия устройства.

Формулировка в UI это отражает: «Ключи хранятся только на этом устройстве».

### Волна 1 · правда и безопасность (2026-06-30)

- **Режим движка честный.** При «локальном» движке сервер не делает ни одного внешнего запроса и отвечает кодом `ENGINE_NOT_CONFIGURED`: локальной модели в сборке ещё нет.
- **Согласие на облако.** Перед первым облачным ходом в профиле продукт показывает, что именно уйдёт наружу; согласие видно и отзывается в «Настройки → Приватность». Каждый облачный ход попадает в ленту событий категории «Приватность».
- **Индекс сейфа — по выбору.** Тумблер «Отправлять индекс сейфа во внешнюю модель»: выключен — наружу не уходят имена, категории и теги файлов.
- **Вход паролем.** Весь `/ai-api/*` закрыт httpOnly-cookie сессии (`APP_PASSWORD`, страница `/login`), лимиты — 10 ходов в минуту и 200 в сутки с адреса, 10 неудачных попыток входа на 15 минут.
- **Секреты — только в окружении.** Токен MCP не хранится в файле и не отдаётся наружу: известен лишь признак «задан». Каталог данных ИИ-слоя вынесен из репозитория переменной `AI_DIR`.
- **Ошибки без внутренностей.** Наружу уходит код каталога и человеческая фраза, детали — в журнал сервера.
- **Метрики без выдумки.** Скорость и требования локального движка показывают «—», пока нет источника.

Переменные окружения (файл `.env`, в git не попадает): `APP_PASSWORD`, `APP_SESSION_SECRET`, `APP_SESSION_TTL_HOURS`, `AI_DIR`, `AI_PROXY_URL`, `EMERGENT_LLM_KEY`, `AI_MODEL`, `NEXT_PUBLIC_AI_MODEL_LABEL`, `AI_RATE_PER_MIN`, `AI_RATE_PER_DAY`, `MCP_NOTION_TOKEN`.

### Волна 4 · зрелость и рост (2026-06-03)

- **Демо названо демо.** Показательные объекты помечены плашкой «демо», собраны в отдельный модуль вне основного бандла и убираются одной кнопкой «Начать с чистого сейфа»; пользовательские данные при этом не трогаются, а в сейф с чужими данными демо не подмешивается вовсе.
- **Клавиатура и скринридер.** Один хук на все диалоги: ловушка фокуса, Escape по стеку окон, возврат фокуса на точку входа, `aria-modal`; стрелочная навигация по спискам событий и фильтрам. Контраст неактивных кнопок и мелких подписей приведён к 4.5:1, на 768 px подсказки переносятся, а не режутся. Axe на библиотеке и сейфе — без критичных и серьёзных нарушений.
- **Сводка событий — контейнер.** Склеенные события видны по одному: своё время, метка «новое», переход к источнику; прочитанность не теряется и счётчик не врёт.
- **Одна мутация — один результат.** Приём файлов, переиндексация, импорт секретов и скилл записи пароля проходят через сторож идемпотентности: повтор не выполняется, ошибка откатывается и не молчит.
- **Макет назван макетом.** Скелет MCP спрятан за флагом `mcp.skeleton`, ответы содержат `mock: true` и жёлтую плашку «макет, не реальные данные»; правдоподобные ссылки и выдуманная задержка убраны.

Следующие ступени (за пределами текущего прототипа): IndexedDB + non-extractable CryptoKey, шифрование самого корпуса файлов, синхронизация E2EE между устройствами.

---

## Разработка

```bash
pnpm dev --port 3457        # привычный порт этого каталога
pnpm dev                    # стандартный 3000
npx tsc --noEmit            # строгая проверка типов (должна быть чистой)
pnpm build                  # продакшен-сборка
```

**Практики репозитория:**

- `pnpm-workspace.yaml` держит `minimumReleaseAge: 0` — требует pnpm ≥ 9.
- `next.config.mjs` содержит `allowedDevOrigins: ['127.0.0.1','localhost']` — без него Next 16 блокирует отдачу dev-чанков (страница выглядит «мёртвой»).
- После любых правок: `tsc --noEmit` + проверка `HTTP 200`.
- Эмпирические тесты доски: `node --experimental-strip-types` по сценариям из `react-draggable-boards`.

**Работа с ИИ-агентами** — `AGENTS.md` (авто-подхватывается) и `CLAUDE.md` (→ AGENTS.md): кастомный Next 16 требует чтения встроенных доков перед кодом.

---

## Дорожная карта

- [x] Дизайн-система «Графит» v3 → v4.2
- [x] Доска плиток: drag & drop мышь/тач/клавиатура
- [x] Мастер-ключ (PIN 6 / пароль) + экран блокировки + автоблокировка
- [x] Файловые ключи с wrapped-хранением и пере-упаковкой при смене мастера
- [x] Redact-слой: поиск, чат, инспекторы
- [x] Полная адаптивность 380–2560px
- [x] Менеджер секретов CORE v1: 12 типов, генератор, TOTP, буфер с автоочисткой, импорт/экспорт, бэкапы, Panic Lock+
- [x] Явный демо-режим: плашки, баннер, отключение одним действием
- [x] Доступность панелей и модалок: ловушка фокуса, Escape, стрелки, контраст
- [x] Идемпотентность мутаций: двойной клик даёт один результат
- [ ] Менеджер секретов v1.1: Password Health, expiration-напоминания, BIP39-seed, Wi-Fi QR, вложения, история паролей с откатом
- [ ] Реальный индексатор файлов вместо демо-данных (IndexedDB)
- [x] Реальный стриминг ответа модели (SSE, tool-calling, подтверждение опасных скиллов)
- [ ] Реальный локальный движок вместо облачного (адаптер Ollama / WebGPU)
- [ ] MCP-сервер: управление сейфом из Claude/агентов (ключевая фишка продукта) — сейчас в сборке только скелет за флагом `mcp.skeleton`, его ответы помечены плашкой «макет, не реальные данные»
- [ ] E2EE-синхронизация между устройствами (платный слой)

---

*WorkSpaceX · local ai vault — твой второй мозг остаётся твоим.*
