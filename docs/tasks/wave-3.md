# Волна 3 · Ядро продукта

Цель волны: витрина превращается в работающий сейф с ИИ. Оценка ≈ 3 недели.
Требует закрытых волн 1–2: индексатор без надёжного хранилища бессмысленен.

---

## NF-1 · Реальный индексатор файлов `P1` `TODO`

**Сейчас.** «Добавить файл» создаёт только метаданные, содержимое не читается,
конвейер OCR — таймеры.

**Файлы.** новый `lib/indexer/*` + Web Worker · `lib/db/*` (из P0-3)
· `components/screen-library.tsx` · `lib/vault-store.tsx` (addFiles, reindexAll)

**Шаги.**
1. Выбор папки через File System Access API, сохранение handle с разрешением.
2. Worker-пайплайн: чтение → извлечение текста (txt/md/pdf) → чанки → ключевые слова →
   индекс. Тяжёлое не в главном потоке.
3. Хранить в IndexedDB: содержимое, чанки, индекс, хеш файла.
4. Инкрементальность: по хешу переиндексировать только изменённое.
5. Честный прогресс с отменой; статус «в обработке» из реального состояния, не по таймеру.
6. Поиск (`lib/search.ts`) начинает искать по содержимому, уважая redact-слой.

**Готово когда.** Папка на 1 000 файлов индексируется в фоне без блокировки UI,
поиск находит по тексту, повторный запуск обрабатывает только изменения.

---

## NF-2 · Локальный движок `P1` `TODO`

**Файлы.** новый `lib/llm/*` (интерфейс `LlmProvider`) · `app/ai-api/chat/route.ts`
· `components/screen-settings.tsx`

**Шаги.**
1. Интерфейс провайдера: `stream(messages, tools) → AsyncIterable<Delta>`.
2. Реализации: `ollama` (`http://localhost:11434`), `cloud` (текущий прокси),
   позже `webgpu`.
3. Настройка движка выбирает провайдера по-настоящему; недоступный провайдер объясняет,
   что нужно сделать (запустить Ollama, скачать модель).
4. Метрики движка (RM-2) выводить из реального ответа адаптера.

**Готово когда.** При запущенном Ollama диалог идёт без единого внешнего запроса.

---

## AR-1 · Разделение стора и часов `P1` `TODO`

**Проблема.** `lib/vault-store.tsx` — 1550 строк и один контекст; `setInterval(setNow, 1000)`
перерисовывает всё дерево раз в секунду.

**Файлы.** `lib/vault-store.tsx` → `lib/store/{clock,data,notifs,settings,lock}.tsx` · все экраны

**Шаги.**
1. Сначала вынести часы: отдельный `ClockContext`, подписчики — только те, кто показывает время.
2. Затем разрезать по доменам, сохранив внешний API `useVault()` как фасад
   (обратная совместимость важнее красоты).
3. Подписки через `useSyncExternalStore` + селекторы; производные (`graph`, `clusters`)
   считать вне рендера.
4. Проверять React Profiler'ом после каждого шага, а не в конце.

**Готово когда.** Тик секунды не вызывает ререндер экранов без времени; действие
перерисовывает только свою область.

---

## AR-2 · Code splitting экранов `P1` `TODO`

**Файлы.** `app/page.tsx` · `app/globals.css` · `next.config.mjs`

**Шаги.**
1. `next/dynamic` + Suspense на пять экранов (карта и библиотека — самые тяжёлые).
2. Префетч экрана по наведению на пункт сайдбара.
3. Разрезать `globals.css` (11 032 строки) на базу + слои экранов, импортировать со экраном.
4. Замерить bundle-analyzer до и после, цифры записать в PRD.

**Готово когда.** Первый JS-бандл меньше минимум на 40%, переходы без заметной задержки.

---

## NF-4 · Онбординг из трёх шагов `P1` `TODO`

**Файлы.** новый `components/onboarding.tsx` · `lib/vault-store.tsx` · `app/page.tsx`

**Шаги.**
1. Шаг 1: режим приватности (локальный / гибридный) с раскрытием того, что уходит.
2. Шаг 2: создание мастер-ключа (PIN 6 или пароль) — не пропускать молча,
   отказ фиксировать явно.
3. Шаг 3: выбрать папку (NF-1) или «посмотреть демо» (UX-5).
4. Флаг прохождения в настройках; повторный вход онбординг не показывает.

**Готово когда.** Новый пользователь начинает с включённой защитой и понимает,
куда попал; отказ от шагов не оставляет систему в полудоверенном состоянии.

---

## LG-3 · Журнал безопасности `P1` `TODO`

**Файлы.** новый `lib/journal.ts` · `lib/vault-store.tsx` · `lib/secrets-store.tsx`
· `components/security-section.tsx`

**Шаги.**
1. Append-only журнал в IndexedDB: смена мастера, сброс замка, plaintext-экспорт,
   восстановление бэкапа, wipe, «ИИ сохранил пароль», облачные запросы (P0-1).
2. Журнал не чистится из UI и не участвует в retention уведомлений.
3. Просмотр с фильтром по типу, экспорт в файл.
4. Уведомления остаются оповещениями и ссылаются на запись журнала.

**Готово когда.** Все критические действия попадают в журнал, он переживает очистку
уведомлений и экспортируется.
